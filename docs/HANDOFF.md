# 引き継ぎ書 (HANDOFF) — `<日付>` 更新

> **これは時点スナップショットである** (いずれ陳腐化する)。恒久的な運用手順は [ORCHESTRATION_RUNBOOK](conclave/runbook/ORCHESTRATION_RUNBOOK.template.md)、真の進捗記憶は [PROJECT_STATE](PROJECT_STATE.md)。
> 用途: オーケストレーター交代時に「今どこで・次に何をすべきか」を 5 分で把握する。

## 1. 30 秒サマリー

`<プロダクトの現状を 3-4 行。何が動いていて、残るは何か>`

- **ブランチ**: `agent/<task-id>-impl` (最新コミット `<hash>`)
- **品質ゲート**: `<テスト件数 / lint / 型 の状態>`
- **次の自動アクション**: `<スケジュールタスク等があれば>`

## 2. 完成済み (動くもの)

| フェーズ | 状態 | 証跡 |
|---|---|---|
| | ✅/🔄 | |

## 3. 残作業 (完成までの正確な経路)

```text
<人間ゲートを ★ で明示したフロー図>
```

**重要**: AI が代替できない人間ゲートを明記する。「準備完了・実行待ち」が規則内で到達できる最大状態のとき、それ以上を発明しない。

## 4. 人間判断待ち (材料は準備済み — 人間の入力のみ必要)

| 判断 | 準備済み材料 |
|---|---|
| | |

## 4.5 この期間の主要決定 + 却下した代替案 (full trace の保全)

> 「結果」だけでなく「**なぜ**」を残す。要約は lossy で暗黙の決定を落とす(Cognition)。後続が WHAT だけでなく WHY を復元できるように、決定と**却下した代替案 + その理由**を記録する。

| 決定 | 採用理由 | 却下した代替案 | 却下理由 |
|---|---|---|---|
| | | | |

## 5. 引き継ぐ人/AI が最初にやること

1. [ORCHESTRATION_RUNBOOK](conclave/runbook/ORCHESTRATION_RUNBOOK.template.md) §1 の起動シーケンスを実行。
2. §2 決定木で現在地を判定。
3. `<プロジェクト固有の確認手順>`
4. 人間ゲートに当たったら停止し、本書 §4 の材料を添えて人間に渡す。

## 6. 環境メモ

- 実行: `<コマンド>`
- 必要なキー (名前のみ・値は管理外): `<ENV_VAR 名一覧>`
- 外部 AI CLI の導入・稼働状態: `<...>`
