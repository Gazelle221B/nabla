# プロジェクト状態  (最終更新: 2026-07-24 / 更新者: `IMPLEMENTER` (Claude Code Sonnet, M9b 前提チェック関門パイロット実装))

> ★ **これが真の記憶である。** 全エージェントが随時更新する。大コンテキストモデルの内部記憶を真実の源にしない。
> どのエージェントが落ちても・交代しても、このファイルを読めば継続できる状態を保つ。

## 現在のフェーズ

**★★ M9「計測と構造化」開始(2026-07-24、ADR-006・Issue #49、制作者 Go 済み)。公開32単元+単元マップ。** 全マイルストーン(MVP 1〜3+M4〜M8)+バックログ完走を受け、重心を「生産」から「検証・構造化・流通」へ移す。被験者検証は実施せず、GA4 行動テレメトリによる破綻検出へ適応(効果の証明とはしない)。M9a(計測基盤)→ M9b(前提チェック関門)→ M9c(演習+予想履歴)→ M9d(流通)。以下は歴史的記録:

**★ Tier 1 マイルストーン(M4〜M7)完走(2026-07-13)。公開15単元。** MVP 1(2026-07-12 完了)に続き、ROADMAP の M4(一次関数/二次関数/三角比)・M5(相似/円周角/正弦・余弦定理)・M6(導関数/定積分/数列)・M7(内積/一次変換と行列式/確率)の12単元を、各単元とも「Sonnet 実装(隔離worktree)→ GrokBuild 独立レビュー + Antigravity 数学QA(異3系統)→ 指摘反映 → CI → マージ → 本番デプロイ」の完全サイクルで統合。**次フェーズ(MVP 2=Tier 2 / MVP 3=Tier 3)は Tier 導入 ADR + 人間 Go が着手ゲート(ADR-003)のため、自律進行はここで停止。** 従前の記録: **★ MVP 1 完了(2026-07-12)。** M1(三平方の定理)・M2(微分係数と接線)・M3(2×2行列と固有ベクトル)の全 PR(#1〜#7)+ 最終仕上げ PR #8(ホーム導線+トップ a11y 是正)を main へマージ済み(main `90e64fe`)。統合後 main で全品質ゲートをフレッシュ検証し回帰ゼロ、DoD の全項目を**本番デプロイ実物で実測充足**(Lighthouse Accessibility 全4ページ 100・公開 URL 200・JS 無効可読)。3記事すべてが「教材/操作/技術」DoD を満たし、MVP 1 完了条件(IMPLEMENTATION_PLAN §完了の定義)を充足。

- **統合検証(2026-07-12、統合 main 93600d4 上でフレッシュ実行)**: `typecheck` 0 errors(29 files)/ `lint` clean / `test`(Vitest+fast-check)**140 件全緑**(8 ファイル)/ `build` 5 ページ / `test:e2e`(Playwright+axe)**13 件全緑**(3記事すべてで axe Critical/Serious 0・コンソール例外 0・予想→操作→観測フロー)。M1/M2/M3 の統合による回帰ゼロ。
- **Lighthouse Accessibility 実測(2026-07-12)**: 3記事ページ = 100、トップページ = 94(唯一の減点 `landmark-one-main`)。PR #8 で `<main>` 追加により **全4ページ 100** を達成。DoD「Lighthouse Accessibility 原則100」を推論ではなく実測で closure。
- 実装済みの成果: 数学モデル `lib/math/{pythagoras,derivative,eigen,compare}.ts`(純粋 TS、不変条件テスト)+ Mafs 図解 `scenes/mafs/{Pythagoras,Derivative,Eigenvector}Scene.tsx` + 対話 Island `lesson/{InteractiveExperiment,DerivativeExperiment,EigenvectorExperiment}.tsx` + content collection 記事3本 + 動的ルート `[slug].astro` + C-2 実在性テスト。
- 品質ゲート基盤(T5-1): ESLint(flat config)、`.github/workflows/{ci,deploy}.yml`、Playwright + `@axe-core/playwright` E2E。CI・Deploy とも main で success。
- リポジトリ公開: https://github.com/Gazelle221B/nabla 。GitHub Pages: https://gazelle221b.github.io/nabla/ (トップ+3記事すべて **200** 実配信確認、記事はサーバー HTML に KaTeX 数式+`<noscript>` フォールバックを含み JS 無効でも本文・数式が読める)。
- conclaveガバナンス一式(`AGENTS.md`, `docs/conclave/*`, `prompts/*`)導入済み。

## 作業中ブランチ

- `agent/m9a-metrics-impl`(PR #52、未マージ)。M9a 計測基盤。独立レビュー(Kimi K2.7)の指摘3件反映済み。次: 再レビュー/QA → 統合。
- `agent/m9b-prereq-check-impl`(隔離 worktree、新規、未マージ)。M9b 前提チェック関門(パイロット3単元)。IMPLEMENTER 実装完了・全ゲート緑、独立レビュー(別系統)・QA_MEMORY(数学/学習設計)は未実施。オーケストレータの統合待ち。詳細は改訂履歴・下記「M9b 実装記録」参照。
- PR #54(ブランチ `agent/m9c-exercises-history-impl`、未マージ)。M9c 演習+予想履歴(パイロット3単元)。IMPLEMENTER 実装完了・全ゲート緑、独立レビュー(別系統)・QA_MEMORY(数学/学習設計)は未実施。オーケストレータの統合待ち。詳細は改訂履歴・下記「M9c 実装記録」参照。

(過去の M4「二次関数とグラフ」ブランチは M4〜M7 完走〔2026-07-13〕で既に統合済み。この行はその後の更新漏れだったため、本改訂で現状に合わせて是正した。)

## 直近の設計判断

- レンダリング戦略: Mafs/SVG段階導入案とWebGPU必須案(ADR-001原案)の対立を、Tier 1〜3のハイブリッドで解消(`docs/DESIGN.md` §レンダリング戦略、`docs/PROJECT_PLAN.md` §4)。
- **Tier 3をさらにThree.js(Tier 3a・標準3Dシーン)とWebGPU(Tier 3b・シェーダー品質/大量計算3D)へ分割**(ユーザー指示:「Three.jsも適材適所で使う」)。スレッドAが元々Tier 3候補としていたThree.jsを、単純にWebGPUへ置き換えず併存させた。3DはまずTier 3a(Three.js)で足りるか検討し、密な計算・シェーダー品質が本質的に必要な場合のみTier 3b(WebGPU)へ進む。WebGPU必須・フォールバックなしはTier 3bのみに適用され、Tier 1・2・3aは全ブラウザ対応を維持。反映先: `AGENTS.md` §3・§5、`docs/DESIGN.md`、`docs/REQUIREMENTS.md`、`docs/PROJECT_PLAN.md`。
- 開発ガバナンスとしてconclaveフレームワークの採用を確定(本更新で導入完了)。
- AIロール割当をconclaveの5スロット(HUMAN/ARCHITECT/IMPLEMENTER/REVIEWER/QA_MEMORY)へ再編。nabla側の原案(Claude Code=実装+コードレビュー)はconclaveのacting/judging分離原則(実装者≠レビュアー)と衝突するため、REVIEWERをCodex、QA_MEMORYをGeminiに分離した(`AGENTS.md` §7)。
- T1-2で `markdown.processor: unified({...})` API(Astro v7新形式)を採用。非推奨だった `markdown.remarkPlugins`/`markdown.rehypePlugins` の直接指定を廃止。
- **T3-1: Tier 1 図解ライブラリに Mafs(`mafs@^0.21.0`、MIT)を採用**(`docs/adr/ADR-002.md`、DESIGN §レンダリング戦略の Tier 1 既定に沿う)。数学は `lib/math`(純粋 TS)に残し、Mafs は描画層 `components/scenes/mafs/` のみに限定。
- **T3-1: 状態の単一の源(SSOT)を親 Island に一元化**。可動点は controlled な `MovablePoint` + 制約関数(直角維持 + `[1,5]` クランプ)で、ドラッグ・スライダー・数値入力すべてが親の `handleLegA/B` を通る。`useMovablePoint` フック(内部状態を持つ)は SSOT を二重化するため不採用。
- **T3-1: 残差の「≈0」判定を `lib/math/compare.ts`(`EPSILON`/`approximatelyZero`)に切り出し**、MATH_CONVENTIONS §2 のスケール相対誤差を描画層から使えるようにした(具体例+不変条件テスト付き)。負スケールは大きさで扱う(`Math.max(1, Math.abs(scale))`)。
- **T3-1: Mafs はマウントまで描画しない(SSR で図が出ない)ため、`<noscript>` フォールバック文言 + 静的な定理記述を用意**(JS 無効でも本文と数式が読める。ビルド後の静的 HTML で出力を確認済み)。
- T3-1: コンポーネント結合テストのため devDependency に `jsdom`/`@testing-library/*`/`@vitejs/plugin-react` を追加し、`vitest.config.ts` を jsdom 環境+React 変換プラグインに更新(T5-1 の `exclude: e2e/**` と統合)。
- T3-1: 数値入力は `type=text`+`inputMode=decimal` にし表示用ローカル文字列 state を持たせ、確定(blur/Enter)時に clamp して数値 state へ反映(`type=number` はブラウザが "1." 等の入力途中を空へ正規化するため)。

## 未解決リスク

- Tier 3a(Three.js)・Tier 3b(WebGPU)それぞれの初回導入対象単元は未確定(MVP 3着手時に選定、`docs/DESIGN.md` §オープン論点)。
- インタラクティブ図解の作り込みコストが高く、横展開時のスケールがボトルネックになりうる(既知リスクとしてREQUIREMENTS.mdに記載済み、対策は図解コンポーネントの再利用パターン確立)。
- devDependency `@astrojs/check` の依存先(`yaml`パッケージ、`yaml-language-server`経由)にmoderate severityの脆弱性(deeply nested YAML collectionsによるstack overflow、GHSA-48c2-rrv3-qjmp)が`npm audit`で検出されている。開発時の型チェックツールのみが依存し、ビルド成果物には含まれず、当プロジェクトが任意のYAML入力を解析する経路もないため実害は低いと判断し、`npm audit fix --force`(breaking change)は保留した。

## レビューの直近結果

- **MVP 1 統合検証 (2026-07-12、ORCHESTRATOR=Claude Code Fable 5)**: M1/M2/M3 は別ブランチ・別 worktree で実装・個別マージされたため、統合 main HEAD での回帰有無が本質リスク。全ゲートをフレッシュ実行して**回帰ゼロ**を確認(unit 140 / e2e 13 / typecheck 0 / lint / build 5p)。加えて Lighthouse Accessibility を全ページ実測し、トップの `landmark-one-main` 減点(94)を発見 → PR #8 で是正。
- **PR #8 (ブランチ `agent/homepage-nav-a11y-impl`、ホーム導線+トップ a11y)**: 実装=ORCHESTRATOR。独立 REVIEWER=Codex(実装者≠レビュアー)へ差分レビュー依頼、GitHub Copilot コードレビューを PR にリクエスト済み。数学・記事本文の変更を含まないため QA_MEMORY(数学/学習設計)の追加検証は非該当。全ゲート+Lighthouse 全4ページ 100 をフレッシュ確認。
- **T3-1 (ブランチ `agent/t3-1-impl`、PR #4)**: 独立レビュー **codex=CONCERNS / antigravity=CONCERNS**(数学的正しさ・学習設計・noscript・ADR-002 は QA 側で明示 PASS)。統合指摘5件を1ラウンドで修正済み: (1) 数値入力の編集途中破壊 → 表示用ローカル文字列 state + 確定時 clamp、`type=text`+`inputMode=decimal`。(2) 予想確定時のフォーカス喪失 → `useRef` で a スライダーへフォーカス移動。(3) `compare.ts` を `Math.max(1, Math.abs(scale))` に。(4) CSS `.noscript p` → `.noscript`。(5) スライダー `aria-labelledby` + 数値入力 `aria-describedby`。修正後 `typecheck`/`build`/`test` フレッシュ緑(39テスト)。最終工程で最新 main を追従 merge し、E2E スモークを /lessons/pythagorean-theorem へ拡張(コンソール例外0・axe Critical/Serious 0・予想→操作→残差の基本フロー)。
- **T5-1 (PR #3、マージ済み)**: REVIEWER(codex)=FAIL・QA_MEMORY(antigravity)=CONCERNS の初回判定を受け、有効指摘6件を1ラウンドで修正・再検証。人間/オーケストレータ承認を経て main へマージ済み。
- **T1-2 & T2-1 (PR #1、マージ済み)**: `REVIEWER`(Codex/GPT-5.5 xhigh)・`QA_MEMORY`(Antigravity/Gemini)ともに **PASS**。Copilot指摘8件対応済み。詳細: `docs/REVIEW_REPORT.md` / `docs/QA_REPORT.md`。

## 次に実行すべきアクション

**★ 現フェーズ = M9「計測と構造化」(ADR-006、2026-07-24 制作者 Go 済み)。** 方針の全文は Issue #49 と ADR-006 を参照。

1. **M9a: 計測基盤** — PR #52(ブランチ `agent/m9a-metrics-impl`、未マージ)。`docs/METRICS_PLAN.md` 事前登録 → GA4 カスタムイベント(予想入力・予想確定・操作実施・単元完了)を全32単元へ配線 → 独立レビュー(Kimi K2.7)の指摘3件(GraphTheoryExperiment の検出漏れ・構造契約回帰テスト・スコープ制限)を反映済み(実測 ID は HUMAN ゲートのため未発行、現状は全環境で計測無効)。**次: 再レビュー/QA → 統合**。HUMAN タスクとして GA4 プロパティ作成・測定 ID 設定・`deploy.yml` へのシークレット注入が残る(Issue #51 のチェック項目)。
2. **M9b: 前提チェック関門(パイロット)** — ブランチ `agent/m9b-prereq-check-impl`(未マージ)。パイロット3単元(下記「M9b 実装記録」参照)の冒頭に各3問の前提チェックを実装済み・全ゲート緑。**次: 独立レビュー(別系統)+ QA_MEMORY(数学/学習設計)→ 統合**。
3. **M9c: 演習+予想履歴** — PR #54(ブランチ `agent/m9c-exercises-history-impl`、未マージ)。M9b と同一のパイロット3単元(下記「M9c 実装記録」参照)の末尾に各5問の演習+全32単元共通の予想履歴(localStorage)+専用ページ `/history/` を実装済み・全ゲート緑。**次: 独立レビュー(別系統)+ QA_MEMORY(数学/学習設計)→ 統合**。
4. **M9d: 流通の種まき** — 既定学習経路 2〜3 本+教師向け 1 枚ガイド+予想ワークシート。
5. (HUMAN、変わらず)公開ベータの一般告知、商標「nabla / ナブラ」検索・正式命名確定(AGENTS.md §1)。
6. (HUMAN、残置)被験者による教育効果検証 — M9 では実施しない(ADR-006)。テレメトリは破綻検出のみで「検証済み」表示はしない(C-5)。

## 人間判断待ちの事項

| 判断 | 準備済み材料 |
|---|---|
| **MVP 1 完了の追認(事後監査)**(マージ自体は C-1/§8 によりオーケストレータが実施可。制作者は GitHub 上で事後監査・随時 revert 可) | 全 DoD 項目の実測証跡(unit 140 / e2e 13 / Lighthouse a11y 全4ページ 100 / 公開 URL 200 / JS 無効可読)。3記事の数学的正しさは実装者と別エージェント(codex/antigravity)で検証済み(C-5)。 |
| **公開ベータの一般公開 / 商標「nabla・ナブラ」検索**(AGENTS.md §1: 正式公開前に商標検索) | サイトは技術的に公開状態(GitHub Pages 200)。一般告知・命名確定は HUMAN 判断。 |

## 改訂履歴

| 日時 | 更新者 | 変更 |
|---|---|---|
| 2026-07-02 | HUMAN + Claude Code | meeting.md/meeting2.md からdocs/PROJECT_PLAN.md(統合計画書)を作成 |
| 2026-07-02 | HUMAN + Claude Code | レンダリング戦略をハイブリッド(Tier1〜3)で確定 |
| 2026-07-02 | ARCHITECT(Claude Code) | conclaveガバナンス導入。AGENTS.md/REQUIREMENTS.md/DESIGN.md/IMPLEMENTATION_PLAN.md/PROJECT_STATE.md を作成 |
| 2026-07-02 | ARCHITECT(Claude Code) | Tier 3をThree.js(3a)/WebGPU(3b)へ分割。全ガバナンス文書を同期 |
| 2026-07-02 | ARCHITECT(Claude Code) | T0系タスク完了。`MATH_CONVENTIONS.md`、`LICENSE`系、`DEVELOPMENT.md`、`CONTRIBUTING.md`、`docs/adr/ADR-001.md`+`INDEX.md` を作成 |
| 2026-07-02 | IMPLEMENTER(Claude Code Sonnet) | T1-2完了: React+MDX+KaTeX統合。T2-1完了: `src/lib/math/pythagoras.ts` + Vitest+fast-check 不変条件テスト(20件全GREEN、seed=42)。ブランチ `agent/t1-2-t2-1-impl`。 |
| 2026-07-02 | QA_MEMORY(Gemini) | T1-2+T2-1 (PR #1) のQAレビュー完了。QA_REPORT.md を作成し、品質ゲート判定を PASS とする。 |
| 2026-07-02 | ARCHITECT(Claude Code) | ゲートのプロセス監査: ラッパー経由の初回レビュー/QA試行を無効と判定し破棄。codex exec と agy を直接実行し両ゲート正規PASS。REVIEW_REPORT.md作成 |
| 2026-07-04 | HUMAN + オーケストレータ | PR #1をmainへマージ。 |
| 2026-07-04 | HUMAN + Claude Code(オーケストレータ) | マージ権限を委任(レビューPASS+QA PASS+Copilotレビュー依頼済みが条件、制作者は事後監査)。AGENTS.md C-1/§8・DEVELOPMENT.md §6・CONSTITUTION.md・ORCHESTRATION_RUNBOOK を整合更新(PR #2としてmainへマージ済み)。 |
| 2026-07-04 | IMPLEMENTER(Claude Code Sonnet) | T5-1完了: ESLint(flat config)・`.github/workflows/ci.yml`・Playwright+`@axe-core/playwright` E2Eスモーク基盤。レビュー指摘6件対応後、PR #3としてmainへマージ済み。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T3-1完了: `InteractiveExperiment`(予想ゲート/制約付き可動点/スライダー+数値入力+矢印キー/残差ライブ表示/リセット/noscriptフォールバック)+ `PythagorasScene`(Mafs)+ `lib/math/compare.ts`。Mafs 0.21.0 採用(ADR-002)、LICENSES.md 同期。ブランチ `agent/t3-1-impl`(PR #4) |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T3-1 レビュー(codex/antigravity=CONCERNS)の統合指摘5件を修正: 数値入力の編集途中保持/確定時フォーカス移動/`compare.ts` の `Math.abs(scale)`/CSS `.noscript`/スライダー `aria-labelledby`。テスト39件緑 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T3-1: origin/main(PR #3 マージ後)を追従 merge(vitest.config は jsdom+setupFiles と exclude:e2e を統合、package.json devDeps は両立)。E2E スモークを /lessons/pythagorean-theorem へ拡張(コンソール例外0・axe・予想→操作→残差フロー)。全ゲートフレッシュ再確認。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T3-1 Copilot 指摘対応(6件): `compare.ts` を非有限入力で RangeError(MATH_CONVENTIONS §3、テスト更新)/ E2E の「コンソール未処理例外0」を console.error 収集+アサートに格上げ(2件)/ ADR-002・INDEX のステータスを「Accepted(PR #4 マージで確定)」に統一。全ゲート(typecheck/lint/build/test 39件/test:e2e 6件)フレッシュ緑。T3-1 は PR #4 として main へマージ済み。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T4-1(三平方の定理の記事、ブランチ `agent/t4-1-impl`/PR): content collection 導入(`src/content.config.ts` curriculum スキーマ C-2 準拠、`zod` 直接 import)+ 記事 MDX(`src/content/lessons/geometry/pythagorean-theorem.mdx`: 学習目標2/操作前予想/代表的誤解/転用問題/JS無効でも本文・数式可読)+ 動的ルート `[slug].astro`(検証ハーネス .astro を置換)。スライダーと数値入力のアクセシブルネームを区別。全ゲート(typecheck 0/ lint/ build/ test 39/ e2e 6)フレッシュ緑。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | T4-1 レビュー(codex/antigravity=CONCERNS)の指摘7件を修正: zod を `astro/zod` へ(直接依存削除)/ C-2 実在性の unit テスト追加(全 lessons 走査)/ `[slug].astro` に slug 衝突ビルド時 throw / E2E を data-hydrated 待ち+ビューポート拡大で堅牢化 / 記事の鋭角鈍角の係り受け修正・「残差」→「差」平易化(コンポーネント UI も同期)・JS無効ナビ noscript 追加。全ゲート(typecheck 0/0/0・lint・build・test 42・e2e 6)フレッシュ緑。PR #5 として main へマージ済み。 |
| 2026-07-04 | IMPLEMENTER(Claude Code, 隔離worktree `nabla-m3`) | M3完了: `lib/math/eigen.ts`(2×2行列の作用・固有値/固有ベクトル・4状態分類・平行判定・符号連続性の3分割API)+ `EigenvectorScene`(Mafs)+ `EigenvectorExperiment`(予想ゲート/単位円上の可動点/角度スライダー+数値入力/行列プリセット切替/残差・平行度ライブ表示)+ 記事ページ `src/pages/lessons/eigenvectors.astro`。単位行列・回転行列の誤分類事故(AGENTS.md §7)の回帰テストを含む不変条件テストを追加。ブランチ `agent/m3-impl`(PR #7)。レビュー(codex)指摘に基づき判別式を数値安定形 (a−d)²+4bc へ書き換え・分類を判別式符号のみで判定(epsilon幅撤廃)・重解時の下位分類も exact zero 判定へ変更・isParallel をオーバーフロー耐性のある正規化へ変更。全ゲート(typecheck/lint/build/test 100件/test:e2e 9件)フレッシュ緑、新規依存なし。 |
| 2026-07-04 | IMPLEMENTER(Claude Code, 隔離worktree `nabla-m3`) | M3最終統合: T4-1 と同じ content collection 方式へ記事を移行(`src/pages/lessons/eigenvectors.astro` を削除し `src/content/lessons/linear-algebra/eigenvectors.mdx` へ、frontmatter は `content.config.ts` の independent curriculum スキーマ準拠・learningGoals 2 個・prerequisites 空配列)。描画は動的ルート `[slug].astro` に一本化、URL は `/lessons/eigenvectors/` のまま維持(E2E で確認)。全ゲート(typecheck/lint/build/test 105件・C-2 実在性テスト含む/e2e 9件)フレッシュ緑。 |
| 2026-07-04 | IMPLEMENTER(Claude Code, 隔離worktree `nabla-m3`) | 再QA(Gemini)指摘のテキスト3件のうち2件を修正: 固有値の定義を負の固有値でも破綻しない表現に(向きが反対の場合は負の符号をつける旨を補足)/ 転用問題の解答の式変形順序を因数分解→=0の自然な流れに修正。誤字1件は現行ファイルに該当箇所なしと判断(対応不要)。build/typecheckフレッシュ緑。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | M2完了: `lib/math/derivative.ts`(差分商・微分係数・接線/割線、非有限入力は RangeError)+ Vitest/fast-check 不変条件テスト17件(割線が h→0 で微分係数に収束/割線・接線が定義点を通る、など自己確認的でない検証)。`DerivativeScene`(Mafs)+ `DerivativeExperiment`(予想ゲート/可動点+h スライダー/数値入力/観察テーブル/noscript、InteractiveExperiment と同パターン)+ 記事 `src/pages/lessons/derivative-tangent-line.mdx`(学習目標2個・誤解「h=0を代入」・転用問題)。E2Eスモークを同ページへ拡張(9件全緑、client:visible ハイドレーションのタイミング起因フレークを対処: scrollIntoViewIfNeeded の交差率境界問題をビューポート拡大で構造的に解消)。全ゲート(typecheck/lint/build/test 68件/test:e2e 9件)フレッシュ緑。ブランチ `agent/m2-impl`。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | PR #6 レビュー(codex=CONCERNS/antigravity=PASS)の統合指摘4件を修正: (1) 収束の property テストが「h を縮めれば誤差が単調に減る」という一般には偽の不変条件だったため、平均値定理の剰余(誤差 <= \|h\|/2 * 2階微分の上界)に基づく検証へ書き換え(5シード×numRuns=20000で安定性確認済み)。(2) `differenceQuotient`/`secantLine` の approximatelyZero 境界(EPSILON/2)での RangeError を明示テスト。(3) DerivativeScene の viewBox 計算が誤っており(y上限を`f(hi)*0.55`で「縮小」していたバグ)、a・h をともに最大にすると点がはみ出す不具合を修正(`maxH` を新規propとして渡し、y上限を`f(hi)+2`に是正、スクリーンショットで目視確認済み)。(4) E2Eに標準ビューポート+明示スクロールの実ユーザー経路テストを追加。追加中に判明: 前回の「scrollIntoViewIfNeeded の交差率境界問題」という診断は誤りで、真因は「ラジオボタンは同一選択肢の再クリックでは change が再発火しないため、ハイドレーション未接続時の初回クリック消失後は同一ラジオへのリトライが恒久的に空振りする」というネイティブHTML仕様だった。別選択肢と交互にクリックする `selectPredictionRobustly` ヘルパーで解消(5/5成功・初回試行・100ms未満を確認)。全ゲート(typecheck/lint/build/test 71件/test:e2e 10件)をフレッシュ実行し、e2eはフルスイートを5回連続実行して安定を確認。push済み(奇しくも PR #5 の T4-1 側でも同じラジオ change 再発火問題が独立に発見され、`selectPredictionRobustly`/`data-hydrated` の組み合わせで解消されていた。両者一致する診断)。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | PR #5(T4-1、main マージ済み)を追従 merge。記事を `src/pages/lessons/derivative-tangent-line.mdx` から content collection `src/content/lessons/calculus/derivative-tangent-line.mdx` へ移行(curriculum type=mext/stage=high-school/guidelineYear=2018、learningGoals 2件、prerequisites=[]、動的ルート `[slug].astro` に描画を委譲)。`DerivativeExperiment` に `data-hydrated` シグナルとスライダー/数値入力のアクセシブルネーム分離を追加(InteractiveExperiment と同じ形へ統一)。 |
| 2026-07-04 | IMPLEMENTER(Claude Code) | 上記マージ・移行を完了: 記事本文の重複 H1・手書き学習目標ブロックを削除([slug].astro が frontmatter から自動描画するため)、記事末尾に Pythagoras と同型の JS 無効ナビ `<noscript>` を追加。e2e/smoke.spec.ts の `selectPredictionRobustly` 重複定義(T4-1 側と m2 側で同名関数が競合)を1つに統合し、両ページの data-hydrated 待ちを追加。スライダー名変更 (`(スライダー)` 接尾辞) に伴いユニット/E2E テストのロケータを追随。URL は `/lessons/derivative-tangent-line/` のまま(slug 衝突なし)。C-2 の prerequisites テストが新記事を正しく走査することを確認(5件緑)。全ゲート(typecheck 0/0/0・lint・build・test 76件・test:e2e 10件)をフレッシュ実行し、e2e はフルスイートを4回連続実行して安定を確認。push 済み。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | ローカルが 2026-07-02 の古いスナップショットだったため origin/main(93600d4、PR #1〜#7 全マージ済み)へ同期。廃棄済みの ESLint 手動ブランチ(PR #3 で置換)を stash 退避のうえ整理。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | MVP 1 統合検証: 統合 main HEAD で全ゲートをフレッシュ実行し回帰ゼロを確認(typecheck 0/29 files・lint・test 140 件・build 5p・test:e2e 13 件、axe Critical/Serious 0・コンソール例外 0)。GitHub Pages 実配信(トップ+3記事 200、KaTeX 数式+noscript を server HTML で確認)。CI/Deploy とも main で success。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | Lighthouse Accessibility を全ページ実測(記事3本=100、トップ=94)。トップ唯一の減点 `landmark-one-main` を PR #8 で是正: `src/pages/index.astro` に `<main>` を追加(BaseLayout ではなく index 側に置き記事側 `<main>` と二重化させない)、`lang=\"en\"`→`\"ja\"`、公開3単元を content collection から動的に一覧リンク(到達性確保、C-4 スコープ内)。全ゲート+Lighthouse 全4ページ 100 をフレッシュ確認。独立レビューは Codex(GPT 上限で失敗)→ GrokBuild へフォールバック、Copilot レビューを PR #8 にリクエスト。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | PR #8 統合: 独立レビュー GrokBuild=CONCERNS(ブロッカーなし)の有効指摘に対応(未知 stage を fail-loud 化・:focus-visible に outline 追加。DOCTYPE は Astro 自動注入で非該当)。CI(quality-gates)pass・Copilot はリクエスト済み(依頼側 quota で自動レビュー不可)。C-1/§8 の委任に基づきオーケストレータがマージ(main `90e64fe`)。マージ後 CI・Deploy とも success、本番 GitHub Pages で Lighthouse a11y 全4ページ 100・公開 URL 200 を実測確認。**これをもって MVP 1 完了**。 |
| 2026-07-12 | ARCHITECT(Claude Code オーケストレータ) | 制作者「他の単元も全て作成」指示を受け、ポスト MVP 1 の全単元展開を策定。`docs/ROADMAP.md`(ビジョングラフ + M4〜M7 Tier 1・MVP 2/3 で Tier 2/3、3〜5単元/波)+ ADR-003(Tier 1 波状拡張、全単元一括生成は REQUIREMENTS §Current scope 違反として却下)を作成。M4 第1波 = `linear-function`→`quadratic-function`→`trigonometric-ratios`。実装は Sonnet 委譲、レビュー別系統、数学 QA=Antigravity で「この調子」実行。 |
| 2026-07-12 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree) | M4 第1波「一次関数とグラフ」実装完了(ブランチ `agent/m4-linear-function-impl`)。既存 MVP 1 の三平方の定理一式を厳密なテンプレートとして踏襲: `lib/math/linearFunction.ts`(`evaluate`/`yIntercept`/`slopeBetween`/`xRoot`、非有限入力は RangeError)+ Vitest/fast-check 不変条件テスト33件(自己確認的でない: (0,b) を通る・傾き不変性・平行移動/スケール性質・xRoot の根の正しさ・y=2x+1 の既知例・a=0 退化ケース)+ `LinearFunctionScene`(Mafs、傾き点(1,a+b)と切片点(0,b)の2つの可動点)+ `LinearFunctionExperiment`(予想ゲート「a の符号を反転すると直線はどう変わるか」+ a・b それぞれにスライダー+数値入力+矢印キー+リセット、代表的誤解「b を変えると傾きも変わる」)+ 記事 `src/content/lessons/algebra/linear-function.mdx`(中学2年、`prerequisites: []`)。設計判断: `xRoot` は a=0 で RangeError とし、b=0(無数の根)と b≠0(根なし)をメッセージで区別(derivative.ts の `differenceQuotient`/`secantLine` と同じ「ゼロ除算になりうる箇所は専用分岐」方針、MATH_CONVENTIONS §3)。全ゲート(typecheck 0/34 files・lint clean・test 186件・build 6p、`/lessons/linear-function/` 生成確認・home が content collection から自動一覧化を build 出力で確認・test:e2e 16件)をフレッシュ実行し初回で全緑(反復不要)。独立レビュー(別系統)・QA_MEMORY(数学/学習設計)は未実施、オーケストレータの統合待ち。push/PR/マージはしていない。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | 一次関数単元(PR #11)の独立検証: CI(quality-gates)pass。REVIEWER=GrokBuild grok-4.5=CONCERNS(ブロッカーなし、bridge検証済)、QA_MEMORY=Antigravity/Gemini=CONCERNS(数学的誤りゼロ、教育精度3点、bridge検証済)。両者の有効指摘を1ラウンドで反映: 誤解の反証を b 相殺の形へ厳密化 / 学習目標の「水平(a=0)」を定義 a≠0 と整合 / 「x切片・根」→「x軸との交点」(中学範囲)/ テストの approximatelyZero を compare.ts から import / 非有限テスト2件追加(NaN b・Infinity a)/ e2e console-error テストに data-hydrated 待ち追加。全ゲート(typecheck 0/34・lint・test 188・build 6p・e2e 16)フレッシュ緑。Copilot リクエスト済み。 |
| 2026-07-12 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree) | M4 第2波「二次関数とグラフ」実装完了(ブランチ `agent/m4-quadratic-function-impl`)。既存 MVP 1 の三平方の定理一式 + M4 の一次関数一式を厳密なテンプレートとして踏襲: `lib/math/quadraticFunction.ts`(頂点形式 `evaluate`/`vertex`/`axisOfSymmetry`/`completeSquare`、非有限入力は全引数について RangeError)+ Vitest/fast-check 不変条件テスト58件(自己確認的でない: 軸対称性 f(p+t)=f(p-t) / 頂点が極値である a>0→f(x)≥q・a<0→f(x)≤q / completeSquare で得た頂点形式を独立に計算した標準形 ax²+bx+c と突き合わせる整合性 / 頂点の y 値=q / 既知例 / a=0 の completeSquare は RangeError / 非有限は NaN・Infinity・-Infinity を全引数で網羅)+ `QuadraticFunctionScene`(Mafs、頂点点(p,q)と「開き」点(p+1,a+q)の2つの可動点+対称軸の破線)+ `QuadraticFunctionExperiment`(予想ゲート「a の絶対値を大きくすると放物線の開きはどうなるか」+ a・p・q それぞれにスライダー+数値入力+矢印キー+リセット、代表的誤解「頂点の x 座標を −p と取り違える」を x=p 代入で反証)+ 記事 `src/content/lessons/algebra/quadratic-function.mdx`(高校数I、`prerequisites: [algebra/linear-function]`)。設計判断: (1) `completeSquare` は a≈0 で RangeError(p=-b/(2a) がゼロ除算になるため、linearFunction.ts の `xRoot`・derivative.ts の `differenceQuotient` と同じ「ゼロ除算になりうる箇所は専用分岐」方針)。一方 `evaluate`/`vertex`/`axisOfSymmetry` は a=0 でも破綻せず有限値を返す(MATH_CONVENTIONS §4 の退化ケース方針、evaluate 自体は a=0 でも y=a(x-p)²+q=q という有効な式であるため)。(2) 予想の選択肢は「開きが狭くなる/広くなる/位置だけ動く」の3択とし、a の絶対値変化(1→3、符号固定)に限定して曖昧さ(符号を跨ぐ変化だと開閉の向きが変わり「狭い/広い」が単純比較できない)を避けた。(3) 代表的誤解は「頂点の x 座標を −p と取り違える」を選定し、x=p 代入で (x-p)²=0 になり極値に達することを示し、x=-p では p≠0 の限り (x-p)²=4p²≠0 になるため反証する構成にした。全ゲート(typecheck 0/39 files・lint clean・test 260件・build 7p、`/lessons/quadratic-function/` 生成確認・home が content collection から自動一覧化を build 出力で確認・test:e2e 19件を3回連続実行して安定確認)をフレッシュ実行し初回で全緑(反復不要)。既知懸念: `QuadraticFunctionScene` の viewBox は a・p・q の可動範囲全体で頂点・「開き」点が画面内に収まるよう固定して計算しているが、放物線の描画自体は viewBox の外側で自然にクリップされる(Mafs の既定動作、DerivativeScene と同じ扱い)ため、|a| が大きい状態では対称軸から少し離れただけで曲線が視界外に出る(数学的には正しいが、教材的には「開きが狭い」ことを視覚的に強調する副効果として許容)。独立レビュー(別系統)・QA_MEMORY(数学/学習設計)は未実施、オーケストレータの統合待ち。push/PR/マージはしていない。 |
| 2026-07-12 | ORCHESTRATOR(Claude Code Fable 5) | 二次関数単元(PR #12)の独立検証: CI pass。QA_MEMORY=Antigravity/Gemini=**PASS**(数学的誤り・学習設計問題ゼロ、テスト58件を実行検証、bridge検証済)。REVIEWER=GrokBuild grok-4.5=CONCERNS(ブロッカーなし、指摘を実コードと突合、bridge検証済)。有効指摘を1ラウンド反映: (Antigravity) vertex/axisOfSymmetry の JSDoc に a=0 退化(頂点・一意な対称軸が数学的に消失、遷移連続性のため値を返す)を明記 / (GrokBuild C1) vertex/axisOfSymmetry の property が入力エコー(C-7)だったのを `evaluate` 経由の計算検証(頂点 y=頂点 x での関数値・対称軸両側で関数値一致)へ差し替え / (GrokBuild C2) axe e2e に data-hydrated 待ちを追加。全ゲート(typecheck 0/39・lint・test 260・build 7p・e2e 19)フレッシュ緑。Copilot リクエスト済み。 |
| 2026-07-12 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M4 第3波「三角比と単位円」(PR #13)完了 → M4 第1波クローズ**。`lib/math/trigonometry.ts`(`unitCirclePoint`/`sine`/`cosine`/`tangent`/`pythagoreanIdentityResidual`、角度=ラジアン内部統一・度変換は UI 層、`tangent` は cos≈0=θ=π/2+kπ で RangeError)+ 不変条件テスト(恒等式 sin²+cos²≈1・単位円上の点の大きさ≈1・tan は独立実装 Math.tan と突合・周期性・既知値・cos≈0 の RangeError・非有限)+ `UnitCircleScene`(Mafs 単位円+可動角度点+sin/cos射影+弧)+ `TrigonometryExperiment`(予想ゲート+角度スライダー/数値入力/矢印/リセット、cos≈0 で tan「定義されません」を安全表示・クラッシュなし+noscript)+ 記事 `content/lessons/geometry/trigonometric-ratios.mdx`(`prerequisites: [geometry/pythagorean-theorem]`、三平方が sin²+cos²=1 の根拠)。独立検証: CI pass、QA_MEMORY=Antigravity=CONCERNS(数学的誤りゼロ)、REVIEWER=GrokBuild=CONCERNS(ブロッカーなし)。有効指摘を1ラウンド反映: 記事71行の度/ラジアン混在を度数統一(ラジアンは数IIと明記)/ tan プロパティを Math.tan 突合へ(C-7 自己確認解消)/ 閾値外側テストに有限値 assert / 周期性テストに 2π 近似コメント / **axe e2e を予想確定後の操作UI(スライダー/表)も検査する二段構成へ**(予想ゲート型 Island では操作UI が確定後マウントされ確定前 axe では未担保、という全単元共通の穴を是正)。Antigravity の tangent 閾値指摘は EPSILON=1e-9 の誤認に基づくため変更不要と判定。ローカル全スイートはホスト高負荷(load~30)で RTL 結合テストがフレークするが、単独実行では全緑(trig 34・結合3ファイル39)+ e2e 22 緑で、CI(クリーン環境)を権威ある検証とする。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M5 第1波「相似と拡大縮小」(PR #14)** 実装+統合。`lib/math/similarity.ts`(`scaleFrom`=中心拡大 center+k(p−center)/`distance`/`triangleArea`=シューレース、非有限 RangeError、k=0 は除算なしで center 返却)+ 不変条件テスト61件(距離比=\|k\|・**面積比=k²**・中心不動・k=1恒等・既知例・非有限、compare.ts import・非自己確認)+ `SimilarityScene`(Mafs 元三角形+k倍+中心+対応辺)+ `SimilarityExperiment`(予想ゲート+k スライダー/数値/矢印/リセット、辺の比・面積比を実測表示+noscript)+ 記事 `content/lessons/geometry/similar-figures.mdx`(中学3年、prereq [])。代表的誤解「面積比も k 倍」を単位正方形(k=2→面積4倍)で反証。独立検証: CI pass、QA_MEMORY=Antigravity=CONCERNS だが唯一の指摘は引用ミスの false positive(記事は既に「相似比 k=1 のとき合同」と正しい)で数学的誤りゼロ、REVIEWER=GrokBuild=CONCERNS(ブロッカーなし)。有効指摘を1ラウンド反映: (C1) 観察ステータスが実測比を検証せず「一致」断言していたのを approximatelyZero で実行時検証し不一致なら警告表示へ(TrigonometryExperiment と同型の誠実さ)/ (C3) e2e に矢印キー(ArrowLeft)単独操作の検証を追加。C2(辺の比の \|k\| 表記)は C1 の Math.abs(k) 検証で吸収。ホスト高負荷(load~30〜68)で全体スイート・e2e webServer がタイムアウト/フレークするが、typecheck 0/49・lint・相似結合テスト単独12件・数学61件は緑で、CI(クリーン環境)を権威検証とする。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M5 第2波「円周角の定理」(PR #15)** 実装+統合。`lib/math/inscribedAngle.ts`(`pointOnCircle`=radius>0 必須・`angleAtVertex`=符号なし角を `atan2(|cross|,dot)` で数値安定に・ゼロ長は RangeError。中心角/円周角は頂点の選び方で表現し別関数を作らない=Simplicity First)+ 不変条件テスト(タレス=直径→π/2・等円周角・**円周角=中心角/2 を優弧上で**・既知値・非有限/ゼロ長 RangeError、compare.ts import・非自己確認)+ `InscribedAngleScene`(Mafs 円+弦AB+中心O+可動点P)+ `InscribedAngleExperiment`(予想ゲート+P角度スライダー/数値/矢印/リセット、**円周角=中心角/2 を実行時検証してから表示**+noscript。P は drag なし・優弧域215°–445°固定)+ 記事 `content/lessons/geometry/inscribed-angle.mdx`(中学3年、prereq [])。独立検証: CI pass、QA_MEMORY=Antigravity=CONCERNS(数学的誤りゼロ、幾何指摘2件)、REVIEWER=GrokBuild=CONCERNS(ブロッカーなし、スコープ縮小=drag なし+優弧固定を **ACCEPT** と明示判定)。有効指摘を1ラウンド反映: (Antigravity) 記事の優弧説明の反転(「中心 O と同じ側にない」→「同じ側にある」)是正・直径ケースの「優弧」語を「(A,B を除く)円周上」へ / (GrokBuild M1) 同じ幾何誤りが `InscribedAngleExperiment.tsx` のコメントにも転写されていたのを同期是正。L1(コターミナル角UX)/L2(弱いrange property)は任意で見送り。全ゲート(typecheck 0/54・lint・test 442・build 10p)フレッシュ緑(負荷回復後の全スイート、フレークなし)。**運用事故と対処**: 高負荷ピーク時に実装者サブエージェントが起動時ハング(worktree 空で検知)→ 停止・再起動で復旧。GrokBuild レビューも一度ハング疑い→再起動で正常返答(ブリッジログ f380b3bd exit=0 で実行成功を確認)。生存判定は「worktree の実ファイル生成 / ブリッジログ start-end / プロセス有無」で行い、トランスクリプト更新時刻は非フラッシュで信頼できないと判明。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M5 第3波「正弦定理・余弦定理」(PR #16)実装+統合 → M5 波(相似/円周角/正弦・余弦)完了**。`lib/math/lawOfSinesCosines.ts`(`sideLength`/`angleAtVertex`=atan2 方式・共線は 0/π で有効・ゼロ長 RangeError/`lawOfCosinesSide`=√(b²+c²−2bc·cosA)、判別式は証明可能な非負式への丸め保護 Math.max(0,…))+ 不変条件テスト59件(正弦定理比・余弦定理=実距離の独立2経路・内角和π・既知例。三角形は stick-breaking+拡張正弦定理でゼロ棄却生成)+ `LawOfSinesCosinesScene` + `LawOfSinesCosinesExperiment`(SAS=角A/辺b/辺c、a・角B・角C は座標から独立導出、実行時検証+退化安全表示+noscript)+ 記事(prereq [geometry/trigonometric-ratios])。独立検証: CI pass、REVIEWER=GrokBuild=**FAIL→修正→解消**(**H1: 到達可能な UI 入力 b=c∧A=0° で B≡C となり angleAtVertex がゼロ長 RangeError で render クラッシュ**——実装者の全ゲート緑を素通りした本物のブロッカーを独立レビューが発見)、QA_MEMORY=Antigravity=CONCERNS(数学・モデル・テストは PASS、Gemini が内部で Codex/GPT-5.6 Terra にも二重委譲し一致。学習設計2点)。反映: (H1) 退化(sinA≈0)を angleAtVertex 呼び出し前に検出し角B/C を null 化して「定義されません」安全表示+回帰テストを unit(b=c∧A=0)と e2e に追加 / (LD1) 誤解の本文が見出し(直角三角形専用)と逆の誤解を書いていた矛盾を統一 / (LD2) 正弦定理の適用条件を「辺と対角のペア」表現に是正し SSA の曖昧性(解0/1/2個)を注意喚起。M1/L1(Island 検証の入力族・bLen直参照)は lib テストが主担保のため見送り。全ゲート(typecheck 0/59・lint・test 518・e2e 31)フレッシュ緑。ポート4321を別セッションのプロセスが占有していたため、ローカル e2e は一時的な代替ポート設定(未コミット・実行後削除)で実施。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M6 第1波「導関数 — 微分係数から関数へ」(PR #17)実装+統合**。`lib/math/derivativeFunction.ts`(`evaluatePoly`=Horner/`exactDerivativePoly`=係数規則/`toDifferentiableFunction`=既存 derivative.ts へブリッジし differenceQuotient を再利用)+ 不変条件テスト(係数規則を differenceQuotient=独立経路との平均値定理剰余つき収束で検証・線形性・既知例・非有限)+ 二段パネル Scene(f+接線 / f′+点(a,f′(a)))+ Experiment(予想ゲート+a スライダー/数値/矢印/リセット+関数切替 x²/x³、f′(a)↔差分商の一致を MVT 誤差上界で較正した実行時検証)+ 記事(prereq [calculus/derivative-tangent-line]、誤解「f′ は f の一部」を反証)。独立検証: CI pass、REVIEWER=GrokBuild=CONCERNS(**H1級の render 例外漏れは「指摘なし」**=前単元の学びが効いた。C1: cube の誤差上界 9 が差分商の評価点 a+h を覆わず a=aMax で正しいモデルに「異常」警告が出る偽陰性——数値検証で gap 4.50010e-4 > bound 4.50001e-4 を追認)、QA_MEMORY=Antigravity=CONCERNS(数学・モデル・テスト PASS。誤差上界 x²→2/x³→36 の導出も正確と確認。学習設計2点)。反映: (C1) 上界を 6·(aMax+H_FIXED)=9.001 へ+doc 修正 / (C2) cube・a=1.5 で「一致」表示の回帰テスト / (LD1) 形式的定義が実験より前に答え(2x,3x²)を明かしテンプレ順(予想→実験→誤解→定義)から逸脱していたのを再構成、x³ の導関数は転用問題1に温存 / (LD2) 数III 用語「変曲点」を数II の直感表現へ+テンプレ準拠の解答 details 追加。全ゲート(typecheck 0/64・lint・test 559・build 12p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M6 第2波「定積分と面積」(PR #18)実装+統合**。`lib/math/riemannSum.ts`(`riemannSumLeft`=左端点和/`exactIntegralPoly`=係数規則 aᵢ/(i+1)、evaluatePoly を再利用)+ 不変条件テスト41件(収束の誤差上界 (b−a)²·max\|f′\|/(2n)——max\|f′\| は exactDerivativePoly から三角不等式で評価区間全体を覆って導出=前単元 C1 の学び・線形性・区間加法性・既知値 1/3 等・独立経路突合)+ RiemannSumScene(n本の Polygon)+ DefiniteIntegralExperiment(予想ゲート+n 1〜64+関数切替 x²/x+1、収束を同一較正の上界で実行時検証)+ 記事(テンプレ順・prereq [calculus/derivative-function])。独立検証: CI pass。REVIEWER=GrokBuild=CONCERNS(**誤差上界を数値検算し C1 型の再発なし・H1 型例外なしを確認**。重要指摘: 観察の「差」列が検証フラグと連動して UI 到達可能な全入力で「≈ 0」固定になり中核観察〔n増→差縮小〕が成立しない+MATH_CONVENTIONS §8 逸脱)。QA_MEMORY=Antigravity=Gemini 判定は FAIL だったが、**主根拠(approximatelyZero(幅,1) が幅1未満を弾く)は compare.ts 不所持による誤検証で、ブリッジとオーケストレータ双方が実コードで反証・棄却**(実質 CONCERNS: 数学・モデル・テストは PASS)。反映: 差列を常に実値表示(round4、n=64 の ≈0.008 まで見える)+回帰アサーション / learningGoals の 1/3 先出し除去 / Scene 色コメントを実装(Theme.blue/foreground)に同期 / 転用問題2「1の答え」→「問1で求めた答え(1.5)」/ Σ 記号を数II 向けに読み解く注記。**QA 委譲の学び: 依存モジュール(compare.ts 等)を QA 同梱コンテンツに含めること**。全ゲート(typecheck 0・lint・test 615・build 13p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M6 第3波「数列 — 等差数列と等比数列」(PR #19)実装+統合 → M6 波(導関数/定積分/数列)完了**。`lib/math/sequences.ts`(`arithmeticTerm`/`geometricTerm`/`arithmeticSum`、n=正整数強制・非有限全引数 RangeError・d=0/r=0/r<0/a₁=0 は有効退化)+ 不変条件テスト53件(**和の公式↔ループ加算の独立2経路**・階差=d・比=r・**ガウス対称性 aₖ+a_{n+1−k} 一定**・既知値 5050/512)+ SequenceScene(点列+等差の参照直線)+ SequenceExperiment(予想ゲート「点はどんな形に並ぶ?」+タイプ切替+a₁/d(r)。値列=常に実値・検証=ステータス文専用を踏襲)+ 記事(prereq [algebra/linear-function])。独立検証: CI pass。REVIEWER=GrokBuild=CONCERNS(**H1・「≈0」固定・LD1 いずれも再発なしを確認**。M1: 等比の比の分母判定が epsilon 幅で微小項を誤って「定義されません」化→ 数学的に a₁≠0∧r≠0 なら項は0にならないため **exact zero 判定へ**〔M3 固有値分類と同じ判断〕/ L1: 転用問題2の表項数不一致→問題文を表の実態に整合)。QA_MEMORY=Antigravity=CONCERNS(**compare.ts 同梱により誤検証なし**。数学・転用問題・範囲整合 PASS。指摘: テストの許容が手動 1e-6/1e-9 混在→ approximatelyZero を基本にし、累積・増幅を伴う3箇所のみ根拠コメント付き ACCUMULATION_TOLERANCE=1e-6 へ整理 / 「傾き=公差」の明示対応→ aₙ=dn+(a₁−d) の一次式化と対応関係を形式的定義に追記)。全ゲート(typecheck 0/74・lint・test 684・build 14p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M7 第1波「ベクトルの内積」(PR #20)実装+統合**。`lib/math/dotProduct.ts`(`dot`/`magnitude`/`angleBetween`=atan2 方式・ゼロベクトル RangeError)+ 不変条件テスト(対称性・線形性・構成的直交ペア・コーシー・シュワルツ・既知例・非有限網羅)+ DotProductScene(可動2ベクトル+弧+直角マーカー)+ DotProductExperiment(予想ゲート「直角のとき内積は?」+角度スライダー、大きさ固定でゼロベクトル構造的到達不可)+ 記事(余弦定理による2定義の同値性、prereq [geometry/trigonometric-ratios])。独立検証: CI pass。**両レビュアーが独立に同じ C-7 トートロジーを発見**(異系統コンセンサス): QA_MEMORY=Antigravity=FAIL・REVIEWER=GrokBuild=CONCERNS(Medium)——「成分↔幾何の2経路突合」が、angleBetween 内部の成分内積とラグランジュ恒等式 (a·b)²+(a×b)²=|a|²|b|² により恒等式の確認へ潰れ、同型の式誤りを検出できない(既知例テストがバックストップのため実害は低〜中)。反映: (1) テストの参照角を**偏角 atan2(y,x) の差を [0,π] へ正規化した独立経路**に差し替え+angleBetween 自体を偏角差と突合する property を追加 / (2) UI の幾何経路を**スライダー状態の角度差**(成分と真に独立な情報源)から直接計算 / (3) 記事の余弦定理導出に平行・退化ケースの但し書き / (4) learningGoals の公式先出し除去 / (5) Scene コメント誤記(大きさは固定)修正 / (6) atan2 方式の符号なし角が3実装目=rule of three 到達につき横断リファクタを Issue #21 化(単元追加と混ぜない)。全ゲート(typecheck 0・lint・test 745・build 15p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M7 第2波「一次変換と行列式」(PR #22)実装+統合**。`lib/math/linearTransformation.ts`(`applyMatrix` は eigen.ts から再export・`determinant=ad−bc`・`signedPolygonArea`=符号つきシューレース〔向き反転検出のため similarity.ts の符号なし版と使い分け〕)+ 不変条件テスト(det↔変換後シューレース実測面積・一般三角形で面積比=|det|・乗法性 det(AB)=det(A)det(B)・既知例・退化)+ Scene(参照正方形+平行四辺形、det<0 は色+破線の二重符号化)+ Experiment(予想ゲート+a,b,c,d+プリセット4種、det≈0 は連続量として approximatelyZero 判定)+ 記事(independent スキーマ)。独立検証: CI pass。QA_MEMORY=Antigravity=CONCERNS(2経路突合は「極めて堅牢」とPASS。指摘3件を反映: det=0 の退化説明を記事に追加 / 符号反転テストを固定鏡映から**任意行列の property**〔sign(変換後面積)=sign(det)·sign(元面積)〕へ一般化 / **学習順序の逆転指摘**——固有値は det(A−λI)=0 を要するため prereq [eigenvectors] は論理逆転→ prerequisites を [] にし固有ベクトル言及は本文の関連参照へ)。REVIEWER=GrokBuild=CONCERNS(C-2 指摘はブリッジが誤読と反証・棄却。Medium 2件を反映: テストコメント「代数的に独立」の過大主張を「実装経路の分離が検出力の根拠」へ是正 / Experiment のインライン行列積を applyMatrix 再利用へ統一〔SSOT ドリフト解消〕)。全ゲート(typecheck 0・lint・test 785・build 16p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M7 第3波「確率 — 単純な試行と相対度数」(PR #23)実装+統合 → M7 波完了・ROADMAP の Tier 1 マイルストーン(M4〜M7、12単元)完走**。`lib/math/probability.ts`(`createRng`=mulberry32 シード付き決定的 PRNG〔Math.random 不使用〕・`simulateDice`・`relativeFrequencies`・`theoreticalProbability`)+ 不変条件テスト(度数総和=n・相対度数総和≈1・決定性・固定シード n=6000 の決定的 ±0.05・退化/非有限)+ ProbabilityScene(棒グラフ+1/6 参照線)+ ProbabilityExperiment(予想ゲート+n 10〜6000 対数スライダー+「振り直す」=シード+1)+ 記事(新 probability/ ディレクトリ、誤解=ギャンブラーの誤謬)。独立検証: CI pass。QA_MEMORY=Antigravity=CONCERNS(数学・モデル・テスト PASS、誤謬の「希釈」説明は「極めて正確」と評価。用語指摘を反映:「事象」→事柄、「独立」→平易表現+高校用語の予告。「試行」は中学解説の本文語として維持。n=0 ガード指摘は UI 最小値10で到達不能=moot。prereq 追加提案は該当単元未実装のため C-2 上 [] 維持)。REVIEWER=GrokBuild=CONCERNS(mulberry32 実装・off-by-one なし・Math.random 混入なし・hydration 決定性を確認。反映: 決定性テストを**実測黄金値**〔seed=42/43 の既知度数配列と厳密一致〕へ強化=写像規則を含む独立オラクル化 / 再構成テストの役割分担コメント明確化 / **指導要領配置の是正**——2017告示で多数回試行の確率は第1学年のため references を両学年併記へ / 対数スライダーに aria-valuetext 追加。予想前の Scene 表示は全単元共通の確立パターン+n=10 の凸凹棒は答えを明かさないため許容と裁定。ブリッジは scratchpad 衝突による無効実行3回を自己検出し隔離再実行=プロセス誠実性の好例)。全ゲート(typecheck 0/89・lint・test 840・build 17p)フレッシュ緑。 |
| 2026-07-13 | HUMAN + ORCHESTRATOR(Fable 5) | **制作者 Go 受領: M8(Tier 1 バックログ全消化)→ MVP 2(Tier 2)→ MVP 3(Tier 3)の順で進行**(ADR-003 の着手ゲート充足)。M8 はサブ波制: M8a=二次方程式/指数対数/円の方程式、M8b=場合の数/データの分析/確率分布、M8c=数列と極限/テイラー近似+離散数学の具体化判断。matrix-determinant-area は linear-transformation-2d が既カバー、normal-distribution-clt は Tier 2 へ送致。MVP 2 着手時に ADR-004(Tier 2 レンダラー計測・選定)を起草する。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8a 第1波「二次方程式と判別式」(PR #25)実装+統合**。`lib/math/quadraticEquation.ts`(`discriminant`/`realRoots`=D 分類 exact zero・`evaluateStandard`=意図的に completeSquare から独立な直接評価で C-7 循環回避)+ 不変条件テスト(解の代入≈0・個数↔D符号・Vieta・構成的往復 p,q→展開→復元・既知例)+ Scene(放物線+交点マーカー+y切片ドラッグ=上下移動)+ Experiment(a∈[1,3] 整数で退化を構造回避)+ 記事(数I 範囲・複素数不使用)。独立検証: CI pass。**QA_MEMORY=Antigravity=FAIL——記事の転用問題解説の計算誤り(2x²−4x+2=0 の重解を x=4/4=2 と誤記、正しくは 1)を捕捉**。コード・テスト886件・CI がすべて緑でも素通りする種類の誤りで、C-5 の別エージェント数学検証が公開前に機能した初の算術誤り検出。因数分解 2(x−1)² の検算併記で修正。REVIEWER=GrokBuild=CONCERNS(反映: y切片**ドラッグの整数量子化**——連続値だと整数系 D が 0.004 等になり round2 表示「0」と exact 分類「2個」が矛盾〔§8〕+重解 D=0 が測度ゼロで踏めない、の2指摘を1修正で同時解消 / realRoots の素朴実装に数値注記〔UI レンジで桁落ち実害なしを検算済み・レンジ拡大時は再評価〕)。全ゲート(typecheck 0/94・lint・test 886・build 18p)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8a 第2波「指数関数と対数関数」(PR #26)実装+統合**。`lib/math/expLog.ts`(`expBase`=a>0 exact/`logBase`=a>0∧a≠1∧x>0。**a≠1 のみ approximatelyZero**=ln(a)→0 の除算爆発対策、a>0/x>0 は真の定義域境界なので exact——使い分けを文書化)+ 不変条件テスト(**黄金値=主オラクル**〔log₂8=3 等の手計算値〕・往復/指数対数法則/単調性=補助と役割分担明示——両関数が Math.log/pow を共有するため)+ ExpLogScene(exp+log 曲線〔domain を x>0 に制限〕+y=x 破線+量子化ドラッグの対応点/鏡映点)+ Experiment(a∈[1.2,4] で a≈1 構造回避・往復 log_a(a^t)≈t の実行時検証)+ 記事(数II 範囲=e/自然対数/「逆関数」の語も不使用、計算例は再検算済み)。独立検証: CI pass。QA_MEMORY=Antigravity=CONCERNS(計算例は全再検算で正しいと確認。指摘1件を反映: **記事冒頭の指数関数の底条件に a≠1 が欠落**→条件と除外理由〔a=1 は動かない定数関数〕を明記)。REVIEWER=GrokBuild=CONCERNS(反映: C1=a≈1 ガードの JSDoc が「安定性」を過大主張——弾くのは |a−1|≤1e-9 の exact 近傍のみで a=1+1e-8 は通過し ~1e8 倍増幅、真の安全網は UI レンジ、と契約を実態に是正+バンド縁テスト固定〔1±1e-10→RangeError / 1±1e-8→有限巨大値〕/ C2=**0<a<1 レジームの黄金値がゼロ件**→log₀.₅0.25=2 等を追加 / C3=下側近傍 1−1e-13 テスト追加 / C4=Scene が a を自前検証しない旨の防御コメント)。ローカル全体スイートは外部負荷スパイク(load~50)で RTL 14件フレーク、expLog 単独36件は全緑=CI(クリーン環境)を権威検証とする。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8a 第3波「円の方程式と点と直線の距離」(PR #27)実装+統合 → M8a 完了**。`lib/math/circleLine.ts`(`pointLineDistance`/`footOfPerpendicular`/`circleLineIntersections`=quadraticEquation.ts の discriminant/realRoots を再利用。D=4(1+m²)(r²−d²) の代数同値を明記しつつ検出力の根拠は実装分離と文書化)+ 不変条件テスト(交点の**両式代入≈0**=定義への独立オラクル・交点数↔d,r 大小・構成的生成・既知例)+ Scene(円+直線+垂線 d+交点)+ Experiment(**全入力経路を整数量子化**、接する d=r は m=0+整数 k で厳密到達可能——m≠0 は √(m²+1) が無理数のため境界に整数で乗れない旨明記)+ 記事(円の方程式=座標での三平方、誤解2件、転用問題は再検算済み)。独立検証: CI pass。QA_MEMORY=Antigravity=CONCERNS(数学・モデル・テスト・転用問題の検算すべて PASS。反映1件: 予想文「切片 k を小さくする」が k<0 で逆方向になる不正確さ→「k を変えて円へ近づける」へ)。REVIEWER=GrokBuild=CONCERNS(数値検算で **UI 整数格子上に誤分類は存在しない**と確認。反映5件: 接線境界の構成的 property〔単位円上の接線で d≈r+交点は接点近傍——exact D=0 を浮動小数で狙わない頑健設計〕/ footOfPerpendicular の不変条件 property / Scene の固定閾値 0.001 → approximatelyZero へ / prereq に algebra/quadratic-equation 追加〔記事が判別式に踏み込むため〕/ m≠0 で d≠\|k\| の UI テスト)。単独45件+型+lint+build 全緑、CI を権威検証。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8b 第1波「場合の数 — 順列と組合せ」(PR #28)実装+統合**。`lib/math/combinatorics.ts`(`factorial`=n≤18 の根拠 18!<2^53<19! 付き/`permutations`=逐次積/`combinations`=**permutations 非依存の独立乗法式**〔各ステップ C(n,i+1)∈ℤ で丸め誤差なし〕/`enumerate*`=バックトラック列挙・上限5000)+ 不変条件テスト(**列挙個数===公式**=本物の独立2経路・nPr=nCr×r!・対称性・パスカル・無重複。整数演算のみゆえ厳密等価===)+ **DOM 列挙 Scene**(列挙表示が本質のため Mafs 不使用の判断を文書化)+ Experiment(n∈[2,6]/r∈[1,n])+ 記事。独立検証: CI pass。**両レビュアーが独立に同じ構成逸脱(公式・数値例が予想の前=答え先出し)を指摘**(Antigravity=CONCERNS/GrokBuild=高): 予想前を概念+記号のみへ再構成し公式・導出・数値例を形式的定義へ移設+中2確率からの動機づけ導入を追加。その他反映: (C3)「r を 0 に近づけると観察」が UI(r≥1)と不整合→是正 / (C1)算術 property を n≤18 全域へ拡張+上限黄金値(18C9=48620 等)/ (H1 未確定指摘)再クランプは React 18 バッチングで安全と実ソース確認の上、計算直前の防御クランプ safeR も追加して構造的に封殺 / (C4)noscript の結論記載は JS 無効読者への必要情報として許容。数学・モデル・テストは両者 PASS(n≤18 根拠・combinations の整数正確性まで精密検証)。単独47+16件+型+lint+build 全緑、CI を権威検証。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8b 第2波「データの分析 — 平均・分散・相関」(PR #29)実装+統合**。`lib/math/statistics.ts`(`mean`/`variance`=n割り/`covariance`/`correlation`=分散≈0でnull)+ 不変条件テスト(平行移動/スケール/アフィン不変・\|r\|≤1・構成的 r=±1・分散2定義の実装分離突合)+ ScatterScene + Experiment + 記事。独立検証: CI pass。**QA_MEMORY=Antigravity=FAIL(4件目の本物のブロッカー)——散布図の初期配置(固定5点が全て x=3 の縦一列)は、x 偏差ベクトルが可動点のみで決まり相関のスケール不変性により可動点の x をどれだけ遠くへ動かしても \|r\| が不変(符号反転のみ)という数学的欠陥。「外れ値を遠くへ動かすと相関が変わる」という中核体験が x 方向で不成立**。ORCHESTRATOR も独立検証し正確と確認。是正: 固定5点を x 方向に散らした右上がりの雲([1,2],[2,3],[4,4],[5,6],[6,6])へ——(8,8)→r=0.98、(8,−5)→−0.37、(9,−5)→−0.46 と**距離が効く**配置に。連鎖改修: 結合テスト黄金値・e2e(「距離が効く」回帰検証を追加)・記事の到達不能記述。トレードオフ: 全点同一 x は UI 到達不能化(lib テストが null を担保・safe 表示は防御残置)。REVIEWER=GrokBuild=CONCERNS(r 値の静的検算は一致確認も動的性質は見逃し——**両レビュアーの相補性の実例**。反映: Major=Scene が予想ゲート分岐の内側のみでマウントされ本文「下の散布図には…」と矛盾→常時マウント+interactive={submitted} の既存パターンへ / Minor=テストコメントの縦横取り違え是正・ys 側アフィン property 追加)。単独61件+型+lint+build 全緑、CI を権威検証。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8b 第3波「確率分布と期待値」(PR #30)実装+統合 → M8b 完了**。`lib/math/probabilityDistribution.ts`(`expectedValue`=Σx·p/`distributionFromCounts`/`simulateDraws`=**createRng 再利用**・n=0 は標本平均未定義で RangeError〔simulateDice の n=0 許容と意図的区別〕)+ 不変条件テスト(線形性・範囲・**2経路突合**〔割ってから内積 vs 合計して割る〕・固定シード決定的・黄金値 500/6)+ Scene(常時マウント)+ Experiment(はずれ下限1で総本数0を構造回避)+ 記事(誤解=「期待値=最頻値」)。動的性質を実装前に検算(前単元の学び遵守)。独立検証: CI pass。QA_MEMORY=Antigravity=**PASS**(検証スクリプト実行つき: 黄金値・転用問題・収束・テンプレ順・動的性質すべて確認)。REVIEWER=GrokBuild=**FAIL→修正で解消**(5件目のブロッカー級: **「賞金額や本数を変えれば必ず E[X] が変わる」という絶対言明が到達可能な退化状態〔本数0の等級の賞金変更・賞金全0・相殺〕で偽**——動的性質トラップの文言版。是正: 絶対言明を条件付きへ+退化ケースを「期待値は実際に入っているくじだけで決まる」学習機会に転化 / **mdx 内部リンクの base(/nabla)欠落=サブパス DoD 違反**を発見→相対リンクへ / simulateDraws 専用の入力検証テスト追加)。両レビュアーの相補が3度目。単独41+17件+型+lint+build 全緑、CI を権威検証。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8c 第1波「数列の極限」(PR #31)実装+統合(数III 初登場)**。`lib/math/sequenceLimits.ts`(`classifyGeometricLimit`=4分類〔\|r\|<1 収束/r=1 一定/r>1 発散/r≤−1 振動〕**境界 r=±1 は exact**〔除算なしの離散分類、quadraticEquation D=0 と同型〕/`geometricPartialSum`=**r≈1 のみ approximatelyZero で n 分岐**〔(1−r) 除算爆発対策、expLog a≈1 と同型——使い分けを両所で文書化〕/`geometricSeriesSum`=\|r\|<1 のみ・geometricTerm 再利用)+ 不変条件テスト(閉形式 vs 漸化式 S_{n+1}=S_n+r^n の実装分離突合・**厳密誤差恒等式 \|Sₙ−S\|=\|r\|ⁿ·\|S\|**・分類網羅・黄金値 r=1/2→S₁₀=1023/512・非有限 RangeError)+ SequenceLimitScene(点列/部分和切替+RANGE_CAP 安全表示)+ Experiment(r∈[−1.5,1.5] step0.1=4分類+境界±1 が格子点で厳密到達可能)+ 記事(誤解=「0.999…<1」を部分和で反証)。MDX の裸 `<1` パース問題は $...$ 内へ。独立検証: CI pass。**QA_MEMORY=Antigravity=FAIL(6件目のブロッカー級)——Scene 常時マウント方針の盲点: 予想ゲート前から極限の破線+LaTeX「lim aₙ=0」が表示され答えが完全露出**(発見学習の不成立)。是正: `showLimit={submitted}` prop で極限表示のみゲート後に(viewBox は非表示中も極限込みで計算し図の跳ね防止)——**「Scene は常時マウント」と「答えの先出し禁止」の交差点に第3の規範「答えを構成する描画要素はゲートで隠す」を確立**。他2件反映: 「0 に到達することはない」が r=0(格子点)で偽→条件付きへ / 検算は数値実行つき(vitest 独立実行 38/38・誤差恒等式の独立導出・r≈1 近傍緩和の誤差上界 1.05e-8 評価)。REVIEWER=GrokBuild=FAIL→修正で解消(**転用問題4「初項3・公比0.1」は和が 10/3 になる設問誤り〔解答側は 0.3 で正しく設問と矛盾〕——両レビュアーが独立に同一指摘=収束による高信頼**。是正: 初項0.3へ / 「r≤−1 では符号だけが反転」が r=−1.5 で偽〔絶対値も発散〕→r=−1 と r<−1 を書き分け / r≈1 ガードの明示単体テスト追加〔1±1e-12→n 厳密、1±1e-6→閉形式で補正項オーダー検証〕)。数学モデル・0.999…説明・リンク・RANGE_CAP・C-7(スイート全体)は両者 PASS。全ゲート(typecheck 0・lint・test 1219・build 24p・e2e 70=一時ポート4603で実行し config 復元済み)フレッシュ緑。教訓: **並行 worktree 稼働中はローカル vitest/eslint が worktree 内の複製テストを拾う——`--exclude "**/.claude/**"` で本体に限定**(CI はクリーン環境ゆえ無関係)。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8c 第2波「テイラー展開による近似」(PR #32)実装+統合 → M8c 完了**。`lib/math/maclaurin.ts`(`maclaurinCoefficient`=sin/cos/exp/log1p、係数恒等式は exact/`maclaurinPartialSum`=前進ループ加算/`exactValue`=**Math 組込み=独立実装オラクル**)+ 不変条件テスト(Lagrange 剰余上界〔sin/cos: \|x\|¹¹/11!、exp: e·\|x\|¹¹/11!〕・log1p 交代級数上界は **x∈[0,1] 限定**〔x<0 は同符号で交代しない〕・微分の係数恒等式・golden 全 node 再計算)+ **C-7 二重化: lib=前進加算 vs Island=Horner 法の実行時突合**(評価算法の分離)+ TaylorScene(関数別固定 yMin/yMax クリップ=RANGE_CAP 前例)+ Experiment(次数0..12・x クランプ)+ 記事(誤解=「次数↑=どこでも改善」を log1p x=1.5 の誤差 0.68→1.82→6.27 で反証)。ブランチが PR #31 マージ前の main 起点で e2e/smoke.spec.ts が競合→両 describe 併存で解決(**教訓: 単元ごとに同一ファイル末尾へ追記する e2e 方式は並行波の構造的競合点——単元別 spec 分割を Issue #21 と同枠のリファクタ候補に**)。独立検証: CI pass。REVIEWER=GrokBuild=**PASS**(Critical/High 0。転用問題・golden 全再計算一致・C-7 分離妥当。Medium 2件を反映: (1)「次数を上げるほど」の文言が sin x=2 の n=0→1 誤差悪化 0.91→1.09 や偶奇プラトーと緊張→「十分大きな次数での行き先」へ書き換え+非単調性の実例を形式的定義前に明記 (2)log1p x∈(−1,0) のテスト空白は x=1.5 golden では補完されない〔別領域〕→**property (a') 追加: 同符号項の幾何級数優越上界 \|R_d\|≤\|x\|^(d+1)/((d+1)(1−\|x\|)) で x∈[−0.9,−0.01] を Math.log1p 突合**+コメント是正)。QA_MEMORY=Antigravity=FAIL→裁定で1件採用: **「収束するのは \|x\|<1 のときだけ」が数学的誤り(x=1 は交代調和級数として ln 2 に収束)→「−1<x≤1」へ是正+両境界の挙動を明記**(C-5 該当の本物)。指摘「答えの事前露出」は**棄却**——露出箇所は frontmatter(全単元共通慣行)・noscript(PR #28 で許容裁定済み)・実験後セクション(テンプレの設計)のみで、本文の予想前セクションはクリーンと実ファイルで確認(テンプレ規範「予想より前の本文」が禁止範囲)。低=偶奇プラトーの UI 注記は GrokBuild Medium 1 と収束→係数0の段でのみ理由を添える状態依存注記を追加。全ゲート(typecheck 0・lint・test 1287・build 25p・e2e 73)フレッシュ緑。ROADMAP を M8 実績に同期(quadratic-equation/exp-log/circle-line/combinatorics/data-analysis/probability-distribution/limits-sequences=✅、matrix-determinant-area=linear-transformation-2d 既カバー判断を明記、normal-distribution-clt=Tier 2 送致明記)。**離散数学バックログを M8d として具体化: graph-theory-intro(一筆書きとオイラー路)+ recurrence(漸化式と計算量)の2単元**。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8d 第1波「グラフ理論入門 — 一筆書きとオイラー路」(PR #33)実装+統合(discrete-math ディレクトリ新設)**。`lib/math/graphTheory.ts`(`hasEulerPath`/`hasEulerCircuit`=判定式〔孤立無視連結∧奇数次数∈{0,2}/{0}〕・`findEulerPath`=反復版 Hierholzer・多重辺対応・整数演算のみで approximatelyZero 不要)+ 不変条件テスト(判定式↔構成の突合・握手補題 Σdeg=2\|E\| exact・判定式非依存の路チェッカー・奇数次数頂点は偶数個)+ GraphScene(SVG 直書き・Mafs 不使用の判断文書化・**偶奇ラベル/判定はゲート後のみ**=PR #31 規範)+ Experiment(プリセット4種)+ 記事(1736年オイラー、誤解=「複雑なほど不可能」を五芒星 vs 田の字で反証)。実装者が実ブラウザ検証で RTL 不可視の実バグ3件を自己検出・修正(CSS 未定義クラス/垂直水平辺の SVG bbox 幅0で hit 不能→polygon 化/axe nested-interactive→role=group)。独立検証: CI pass。REVIEWER=GrokBuild=**FAIL→修正で解消(7件目のブロッカー級)——Critical: findEulerPath が冒頭で hasEulerPath をゲートに呼び、不変条件 hasEulerPath(g)===(findEulerPath(g)!==null) の偽側が同語反復に(dot-product C-7 違反と同型の2例目)**。是正: 判定式ゲートを除去し、Hierholzer を無条件実行→**構成側の事後検証 consumesAllEdgesExactlyOnce(全辺をちょうど1回ずつ連続使用=オイラー路の定義そのもの)だけで null を決める方式**へ(受理列は定義上オイラー路なので偽陽性は原理的に無い。次数は開始頂点選択のヒューリスティクスのみに使用と文書化)。**High: 記事・学習目標・締めが「奇数次数の個数だけで決まる」と連結条件を脱落**——田の字で辺を複数 OFF すると「奇数2個かつ非連結=不可能」に UI 上到達可能で C-5 抵触→全3箇所に連結前提を明記+締めに「2つの島に分かれると奇数0/2個でも不可能」の観察誘導を追加。Medium 3件反映: 全辺 OFF の判定を trivial ラベル「自明(渡る辺が1本もありません)」へ分離 / 転用2の誘導「OFFにしながら4のまま」が2本以上で偽→「1本だけ」明示+6個になりうる注記 / 生成器の層化不足→**負例 golden 3セル固定(非連結×奇数0=三角形2つ・非連結×奇数2=C4+パス・連結×奇数4=K4)**。Low: BFS 表記→DFS 是正。QA_MEMORY=Antigravity=**PASS**(実行検証つき: 全プリセット次数の手計算一致・Hierholzer 手動トレース・転用問題の独立再導出〔田の字の偶奇二部構造まで〕・ゲート前情報隠蔽の確認。軽微1件反映: checkpoint の「上の図で」がプリセット切替後に不整合→「ケーニヒスベルクの橋にした状態で」と明示+どの橋でも同じ結論の理由を併記)。**教訓: Antigravity は「モデルが正しく動くか」を実行で確かめ PASS、GrokBuild は「検証構造が自己確認になっていないか」を精査し FAIL——同じコードへの相補的視角の最鮮明な実例。C-7 トートロジーは2例とも GrokBuild 検出であり、構成 vs 判定の突合を書くときは「偽側」の独立性まで要確認**。全ゲート(typecheck 0・lint・test 1331・build 26p・e2e 76)フレッシュ緑。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **M8d 第2波「漸化式と計算量 — 素朴な再帰の爆発とメモ化」(PR #34)実装+統合 → M8d・M8 波完了(Tier 1 バックログ全消化、公開25単元)**。`lib/math/recurrence.ts`(`fibonacci`=反復/`naiveCallCount`=C(n)=C(n−1)+C(n−2)+1 のボトムアップ/`memoizedComputationCount`=n+1)+ **C-7: 恒等式 C(n)=2·fib(n+1)−1(帰納法証明を記事掲載)で漸化式直接反復 vs 独立閉形式を n∈[0,75] 全数+fast-check 突合**(整数 exact)+ CallCountScene(DOM 対数スケール棒・メモ化棒は revealed=submitted でゲート・予想は n=30 固定質問+スライダー初期 n=10 で答えのプレビューを構造回避)+ 記事(誤解=「同じ答えなら手間は大差ない」を 269万回 vs 31回=約8.7万倍で反証、subject またぎ相対リンクは flatten ルーティング前例確認済み)。**実装者がオーケストレータの仕様誤りを BigInt 突合で捕捉: 安全上限は n=77 でなく 75(C(76)=2·fib(77)−1 > 2^53。fib(77) 自体は安全域内)——指示を鵜呑みにしない検算文化がパイプライン全層で機能**。独立検証: CI pass。REVIEWER=GrokBuild=**PASS**(Critical/High 0。恒等式の独立帰納証明・全 golden・上限75・倍率86,856倍・11.31時間を再計算一致。C-7 は真の独立2経路と確認。Medium 2件反映: (1)RangeError 文面が「fib(77) が超過」と誤説明→「C(76)=2·fib(77)−1 が超過(fib(77) 自体は安全)」へ是正 (2)n≤2 の退化〔n=1 で素朴1回<メモ化2回と逆転〕が中核メッセージと衝突→数え方の定義差を説明する状態依存注記を観察パネルに追加。Low 1件反映: Scene コメント「両方とも1回」は n=1 で偽→是正。Low 2件は裁定見送り: min-width 2px はラベル正値で些事/memoized property の自己確認性は C-7 主戦場が naive↔閉形式にあり契約確認として許容)。QA_MEMORY=Antigravity=FAIL→**裁定で全5件棄却または既対応**: (1)「誤解セクションの答え記載」=実験後セクションでテンプレ設計の範囲(PR #32 と同型の棄却2例目。予想前セクションは実ファイルで数値漏れなしを確認) (2)退化状態の矛盾=GrokBuild Medium 2 と同一核で対応済み(記事側には該当する絶対言明が存在しないことを grep 確認) (3)対数スケール値1の描画破綻=min-width 2px+実値ラベル+新注記で軽減済みと GrokBuild も設計妥当と評価 (4)予想前の素朴棒(n=10, 177回)表示=PR #23 の確立裁定(Scene 常時表示+答えを構成しない表示は許容)に整合、答えを構成するメモ化棒はゲート済み・スライダーもゲート前ロックで n=30 の値には到達不能 (5)実行時検証表示=全単元共通の C-7 ステータスパターン。全ゲート(typecheck 0・lint・test 1377・build 27p・e2e 79)フレッシュ緑。**M8 総括: 3サブ波+M8d で計10単元(PR #25〜#34)。ブロッカー級検出はレビュー7件(算術誤り・動的性質不成立・絶対言明反例・C-7 トートロジー×2・連結条件脱落・極限ラベル事前露出)+実装者の仕様反証1件。次: ADR-004(Tier 2 レンダラー選定)起草 → MVP 2**。 |
| 2026-07-13 | ARCHITECT/ORCHESTRATOR(Fable 5)+ 計測スパイク(Claude Code Sonnet) | **ADR-004 起票: Tier 2(高密度2D)レンダラーの計測にもとづく選定**(MVP 2 着手ゲート。人間 Go は 2026-07-13 の「Tier1→Tier2→Tier3 GO」指示で充足済み)。計測スパイク(Apple M4・Chromium 149・実 GPU/ANGLE Metal 確認済み・ソース/生データは docs/adr/assets/adr-004-bench/): **エピサイクル型(矢印連鎖 N≤800)は SVG が全条件 143fps で無傷/独立点群は SVG が 5,000 点で劣化開始・20,000 点で 40fps(60fps 割れ)・50,000 点で 14.8fps(破綻)、Pixi.js(WebGL)は 50,000 点でも 143fps・p95 8.5ms**。Mandelbrot 640×480×100iter 純 JS=12.3ms(確定時再計算なら十分)。決定: (1)Tier 2 既定=Pixi.js v8(WebGL・MIT)、WebGPU compute は不採用(互換要件「主要モダンブラウザ全て」に反する——計測以前の要件制約+計測上も不要規模) (2)ピクセル計算は Canvas2D+確定時再計算 (3)昇格閾値=毎フレーム独立更新 ~5,000 要素 (4)**フーリエ級数は計測により Tier 1 維持**(「MVP 2 の題材だから Tier 2」ではなく負荷実測で決める)、LLN/CLT=Pixi、フラクタル=Canvas2D (5)lib/math の描画非依存・a11y DoD は Tier 2 でも削減対象外。独立レビュー(別系統)+Copilot 依頼の PR で発効。 |
| 2026-07-13 | ORCHESTRATOR(Fable 5) | **プロセス障害の記録と是正(PR #35/#36)**: ADR-004 の PR #35 を **CI FAIL のままマージ**してしまった(C-1 のマージ条件違反)。原因は2つ: (1)CI ポーリングスクリプトが FAIL 検出後も無条件に merge を実行する欠陥(break がループを抜けるだけで後続を止めない) (2)ローカル lint を `eslint src e2e` にスコープしていたため、CI の `eslint .` が検出した ADR ベンチ資産(ブラウザ直書き JS)の no-undef を見逃した。実害は quality-gates の Lint 段のみ(サイトのビルド/配信対象外の docs 資産)でデプロイ成果物への影響なし。是正: eslint ignores に docs/adr/assets/** と .claude/**(worktree)を追加するフィックスフォワード PR #36(CI 緑確認後にマージ)。**再発防止規範: (a)CI 判定とマージは同一コマンド連結にしない——PASS を確認してからマージを別途実行 (b)ローカル lint は CI と同じ `eslint .` で実行**(worktree 除外は ignores 側で対応)。 |
| 2026-07-13 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 2 第1波「フーリエ級数 — 回転する円で角ばった波を作る」(PR #37)実装+統合(ADR-004 の計測にもとづき Tier 1/Mafs)**。`lib/math/fourierSquareWave.ts`(`squareWave`=不連続点は左右平均0へ approximatelyZero スナップ/`squareWaveCoefficient`=4/(πk)/`fourierPartialSum`/`computeCoefficientByQuadrature`=合成シンプソン)+ **C-7: 「公式(閉形式) vs 定義(数値積分)」の2経路突合(奇数 k≤25 で最大差 6.7e-10)**+ 不変条件テスト37件(ライプニッツ厳密上界・奇関数性・パーセバル単調接近・**ギブス golden: N=10/25/50 → 1.17981/1.17911/1.17901、(2/π)Si(π)≈1.178980 へ収束**——node 事前検証)+ FourierScene(エピサイクル+波形の2パネル、y=1 参照線はゲート後)+ 記事(誤解=「N を増やせば角も完全再現」→ 各点収束と一様収束の乖離)。独立検証: CI pass。**両レビュアーとも FAIL→修正で解消(8・9件目のブロッカー級)**。QA_MEMORY=Antigravity=FAIL 2件とも採用: (1)予想セクションに係数公式 b_k=4/(πk) が明記=公式先出し禁止(PR #28 規範)違反→定性記述へ (2)**高: T_STEP=0.05 の10進格子where π/2・π が格子に乗らず、t=3.15 が「1π」表示なのに実値は π 超で square=−1 という偽表示**→step を π/60 へ(π/2・π・2π が厳密到達可能)+π 表示を3桁精度「≈」付きへ(数値入力経由の偽表示も防止)。**教訓: 三角関数系のスライダー格子は π の有理数倍にする**(10進格子×π 単位表示は座標系不一致の系統バグ)。REVIEWER=GrokBuild=FAIL: **High=「ピークの高さ≈1.18」と「はみ出し量≈0.18=ジャンプ幅2の約9%」の混同が learningGoals・noscript・誤解節・UI の計10箇所**(1.18 は絶対ピーク、9% は超過分——別の量)→全箇所で分離表記へ。Medium 2件反映: 「最大値は約1.18のまま」が N=1(4/π≈1.27)で偽→条件付き言明+実測値参照へ / UI 走査区間 [0,2π] とテスト [0,π] の同一性主張が不正確→対称性 S_N(t+π)=−S_N(t) を根拠に [0,π] へ統一。Low 2件反映: シンプソン誤差評価が k⁴ 因子を脱落(k=25 で上界≈4.2e-8、実測 6.7e-10 で余裕と明記——ORCHESTRATOR も自らのコメント初稿の 1.3e-7 を再計算で 4.2e-8 に訂正)/「高さは一定」→「わずかに減りながらギブス定数へ漸近」。数学コア・転用問題・エピサイクル SVG 要素数(N=50 で≈100 ≪ ADR-004 合格域)は両者 PASS。全ゲート(typecheck 0・eslint. 0・test 1432・build 28p・e2e 82)フレッシュ緑。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 2 第2波「大量試行 — 大数の法則と中心極限定理」(PR #38)実装+統合(ADR-004 にもとづく初の Tier 2/Pixi.js 単元)**。`lib/math/centralLimit.ts`(Pixi 非依存): `exactSumDistribution`=**厳密分布の整数畳み込みDP=独立オラクル**/`sumMeanVariance`=公式 3.5k・(35/12)k を**有理数 exact**/`normalPdf`/`maxAbsDeviationFromNormal`=KS型統計量/`simulateDiceSums`=createRng 再利用。**C-7: 公式 vs 厳密分布を k=1..12 全てで有理数 exact 突合**+CLT 定量 golden(正規近似最大偏差 k=1→12 で 0.14345→0.03384 単調減少)。CltScene=Pixi v8 ライフサイクル管理+答えオーバーレイのゲート制御+WebGL 失敗時メッセージ(C-3)。a11y 主担保=DOM 観察表(ADR-004 遵守)、RTL は Scene スタブ、E2E は headless SwiftShader で追加フラグ不要。pixi.js は該当ページのみ +119KB gzip(他ページ参照ゼロ確認)。**統合時の学び: worktree 内の npm install はメインリポジトリの node_modules に反映されない——依存追加 PR の統合時は必ず npm install**。独立検証: CI pass。REVIEWER=GrokBuild=**PASS**(厳密分布・2経路・KS定義〔連続性補正なし=真の Kolmogorov 距離と確認〕・6^12 の安全整数余裕・Pixi ライフサイクルを精査。反映: Medium=**init 失敗パスで Application を destroy しない GPU 資源リーク**→catch 内で防御的 destroy / Low 3件=noscript の LLN 文言を確率的限定へ・表ラベル「正規近似との最大偏差」→「厳密分布と正規分布の最大偏差(k のみに依存)」・転用問題2に分散の直接計算 E[S²]=1974/36 を明記・ドット端数「約」表記)。QA_MEMORY=Antigravity=**FAIL→修正で解消(10件目のブロッカー級)——正規近似曲線の横軸が全幅スパンで、列中心基準(両端半列マージン)のヒストグラム・厳密分布輪郭とズレて描画される幾何バグ**(k=1 で左端 53px ズレ。数値でなく幾何解析による検出)→px=columnWidth·(x−k+0.5) へ是正。中1件反映: 転用問題3が「n を動かして縮みを体感」と誘導するのに観察表の σ は n 非依存→**「σ/√n(n に反応)」行を新設**し体感の受け皿に(既存テストのセレクタ衝突も行名先頭を σ に変えて解消)。低2件は裁定棄却: 実験後セクションの内容露出=テンプレ規範(4例目)/seed 42 の非単調推移=確率的現実で、記事が既に「必ずではない」を詳述済み+むしろ LLN の正しい理解の教材。**教訓: Pixi/Canvas 単元の描画幾何(座標変換の整合)は RTL スタブでは原理的に検証不能——レビューの幾何解析と e2e が唯一の網**。全ゲート(typecheck 0・eslint. 0・test 1483・build 29p・e2e 85)フレッシュ緑。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 2 第3波「フラクタル — 拡大しても終わらない図形」(PR #39)実装+統合 → MVP 2 完了(公開29単元)**。`lib/math/mandelbrot.ts`(`escapeTime`=\|z\|>2 脱出の証明コメント付き/`isInMainCardioid`=**閉形式 q(q+(x−¼))<y²/4**/`isInPeriod2Bulb`/`renderEscapeGrid`)。**C-7: 反復 vs 閉形式の非対称オラクル**(閉形式は有限反復では原理的に言えない「永遠に逃げない」を証明できる)——内部 3,400点で失敗0+fast-check。Canvas2D+putImageData・確定時のみ再計算(rAF なし、ADR-004 遵守)・実測 48ms/回。REVIEWER=GrokBuild=**FAIL→修正で解消(11件目のブロッカー級)——High 2件: (1)「黒=マンデルブロ集合」の同一視が記事・UI・Scene 凡例の計5箇所(escapeTime===maxIter は帰属の証明ではなく、境界付近では maxIter を上げると黒→色付きに変わる=UI 到達状態で言明破綻・C-5) (2)転用問題3の解答が「z₂≈2.0003 で脱出」と誤記(正: \|z₁\|=2.0001>2 で1回目に脱出——直前に教えた判定の適用を解答自身が誤る)**。両閉形式判定式の独立導出(吸引固定点→助変数形→代数形/λ=4(c+1))・座標写像の単一定義(前 PR の教訓の観点指定が機能)・ズーム ULP 根拠は PASS。Medium 2件反映: maxIter 限界の明示/プローブ表示桁のズーム連動化。QA_MEMORY=Antigravity=FAIL→裁定: **採用=(A2 高)ズーム上限 10^13 と maxIter≤500 の不整合——深部で全黒つぶれし「複雑さが現れ続ける」が実験上偽になる→上限を反復予算で解像できる 10^6 へ引き下げ+上限到達時に「計算資源の限界であって図形が終わったのではない」を明示**/(A3 中)\|z\|=√(x²+y²) の定義欠落→補記/(A4 中)プローブ初期 (0,0) はカージオイド深部で動きがない→境界の面白い座標例のガイド追加/棄却=(A1 高)実験後セクションの内容露出(規範5例目)。**修正過程で自らバグを混入・自己検出: probeHint の CSS #666 がダーク背景 #1a1d29 でコントラスト 2.92(axe serious)→単独 e2e で捕捉しサイト共通変数 --nabla-text-secondary へ**——修正にも e2e ゲートが効いた実例。数学モデル・golden 軌道・共役対称・カージオイド導出は両者 PASS(Antigravity は判定式の必要十分性まで独立立証)。全ゲート(typecheck 0・eslint. 0・test 1535・build 30p・**e2e 88/88**)フレッシュ緑。ROADMAP 同期(normal-distribution-clt/graph-theory-intro/recurrence/fractal-mandelbrot=✅)。**次: ADR-005(Tier 3a/Three.js 導入)起草 → MVP 3**。 |
| 2026-07-14 | ARCHITECT/ORCHESTRATOR(Fable 5)+ 技術スパイク(Claude Code Sonnet) | **ADR-005 起票: Tier 3a(標準3D)の導入方式 — vanilla Three.js + useEffect**(MVP 3 着手ゲート。人間 Go は 2026-07-13 指示で充足済み)。実装比較スパイク(vanilla vs @react-three/fiber、ソース: docs/adr/assets/adr-005-three-spike/): R3F は純増 **+103.5KB gzip** で、最大の懸念だった**キーボード代替(OrbitControls はオービットのキー操作を持たない)の実装難度が両案で同一**=宣言性の恩恵が a11y 要件に及ばない→ vanilla 採用(Pixi/ADR-004 と同型のライフサイクル規律)。headless e2e は WebGL1/SwiftShader フォールバックで可・**preserveDrawingBuffer: true が非空ピクセル判定に必須**(既定 false では readPixels 常時0の罠)・102 オブジェクトでソフトウェア描画でも 64fps。決定: scenes/three/ 配置・lib/math は Three.js 非依存(Matrix3 等を数学モデルに使わない)・離散カメラ回転ボタン必須・**ドメインカラーリングは「まず ShaderMaterial(WebGL)」で試み、実測で立たない場合のみ Tier 3b(WebGPU)**——「安易に 3D/ピクセル=WebGPU としない」の具体化。独立レビュー+Copilot 依頼の PR で発効。 |
| 2026-07-14 | ORCHESTRATOR(Fable 5)+ REVIEWER(GrokBuild) | **ADR-005 レビュー1回目 FAIL→是正→再レビュー PASS(発効)**。初の「設計判断文書への FAIL」: (High)バンドル計測 +103.5KB の再現資産欠落——ADR-004 が確立した証拠水準(手法・環境・生データ完備)が先例として機能/(High)Tier 3b 昇格ゲートが「立たない」止まりで、C-3(ブラウザ切り捨て)を伴う境界決定の運用基準になっていない。是正: 計測4プロジェクト+README+生ログ収録・**オーケストレータ自身の独立再計測(R3F 純増 101.7KB、初回 103.5KB と ~102±2KB で整合)**・昇格ゲートを客観化(対話中 ≥58fps∧p95≤20ms@640×480 または基準画像比較の系統的アーティファクト、実 GPU 計測を根拠)・ADR-001 例示との関係を「例示は候補」前例で明示・R3F 却下根拠の複合化・drei/a11y fork を代替案へ・preserveDrawingBuffer 本番常時 true 確定。再レビューで全充足+新規重大問題なしを確認(残 Low 2件: 計測プロトコル詳細化は実装 DoD へ/INDEX 表記→反映済み)。**教訓: ADR も コードと同じレビュー水準——「再現できない数値は根拠ではない」**。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 3 第1波「一次変換(3×3)— 空間をまるごと変換する」(PR #41)実装+統合(ADR-005 にもとづく初の Tier 3a/Three.js 単元)**。`lib/math/linearTransformation3d.ts`(Three.js 非依存): 余因子展開/**Sarrus 独立実装**/`multiplyMatrix3`/`transformUnitCube`/**スカラー三重積(外積・内積の独立経路)**+`rotationZMatrix`(後述QA反映)。**C-7: det(m)===三重積(列)——定理そのものの2実装突合**+Sarrus 第3経路+乗法性。立方体トポロジを lib/math 単一定義で Scene と共有(座標系二重実装の構造的回避=PR #38 教訓の適用)。実装者が仕様の穴を自己修正(スライダー範囲 [−2,2] では転用問題の (3,2,1) に到達不能→自由入力 [−6,6])。バンドル 142KB gzip は該当ページのみ。独立検証: CI pass。REVIEWER=GrokBuild=**FAIL→修正で解消(12件目のブロッカー級)——High: three r185 の ArrowHelper はモジュール共有 geometry(_lineGeometry/_coneGeometry)を全インスタンスで使い回すため、disposeGroup の geometry.dispose() が最初の行列更新で共有錐 geometry を破壊し、以後の全矢印が壊れる**(three 本体ソースまで読んだ検出。e2e の canvas 非空判定では検知不能)→ArrowHelper 子は material のみ破棄する方式へ。Medium 3件反映: ArrowHelper.line は Line(LineSegments の親)で material がリーク→Line 系を包括処理/AxesHelper の unmount 破棄欠落→追加/**ready boolean は StrictMode 再実行時に setReady(true) が no-op となり動的グループが空のまま残る latent bug→世代カウンタへ**。Low 2件(面の巻き順=DoubleSide で実害なし・Sarrus golden 追加は任意)は見送り。QA_MEMORY=Antigravity=FAIL→裁定で1採用3棄却: **採用=(A4)回転が固定角プリセットのみで「角度によらず det=1」を連続観察できない→rotationZMatrix+θ スライダー(0..360°、step 5°)を追加**し「くるくる回るあいだ det 表示は 1 のまま」を動的に観察可能に(モデル・テスト・mdx 誘導も追加)。**棄却=(A1)「===厳密比較バグ、200回中130回不一致」——実テストは golden の exact 表現可能値のみ toBe、fast-check は approximatelyZero(L236/253/285)。Antigravity は自作スクリプトの === を検証した藁人形**(実ファイル照合で確定)/(A2)「頂点2は対角」——頂点配列は二進順序で v1,v2,v4 は正しく v0 隣接の3辺/(A3)「導入で答え先出し」——導入は問いの提示形で det への言及なし(規範6例目)。数学モデル・列ベクトル正当性・プリセット golden・転用問題は両者 PASS(GrokBuild は列/行の概念整合まで確認)。全ゲート(typecheck 0・eslint 0・test 1603・build 31p・e2e 90/91=既知 load フレーク1件は単独緑)フレッシュ緑。**教訓: (1)サードパーティの共有リソース(three の module-scope geometry)は dispose 規約の落とし穴——「自分が作ったものだけ破棄する」原則 (2)QA の「実行検証」は対象コードと同条件かの確認が裁定の前提**。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 3 第2波「回転行列と基底変換 — 座標は『ものさし』で変わる」(PR #42)実装+統合(Tier 3a 第2単元)**。`lib/math/rotationBasis.ts`(3軸回転・transpose3・isOrthogonal・`coordinatesInBasis`=Rᵀv〔非直交は null〕・**`solveCoordinates`=クラメルの公式の独立導出**〔特異は null〕)。**C-7: 転置経路(直交専用) vs クラメル経路(一般基底)の突合+適用範囲の非対称検証**(せん断基底=転置 null・クラメル解く/特異=両方 null)。前単元 Three.js パターンを逐語再利用(実装 19分、第1単元 29分から短縮)。独立検証: CI pass。REVIEWER=GrokBuild=**PASS**(受動変換 Rᵀv の符号規約・golden(0,−1,0)・右手系 Rx/Ry・クラメルの列置換を全て独立検算一致。「座標変換の二重実装なし」確認。Medium 1件反映: **クラメル経路の再構成 property B·x̂≈v をランダム可逆基底(回転+せん断合成)全体で追加**——golden 1点依存を解消 / Low 1件反映: Scene ラベルの −0.00 正規化 / Low 1件見送り: 時計比喩の表現は補足済みで誤導リスク低)。QA_MEMORY=Antigravity=**初回委譲失敗→再委譲で成立**: 失敗原因は「実テストを実行して検証せよ」指示に対し依存ファイル(linearTransformation3d.ts)が未同梱でファイル探索10分タイムアウト——**教訓: 藁人形防止(実テスト実行)と実行環境の完結性(依存全同梱+「追加探索不要」明記)はセットで指定する**。再委譲は実テスト30件をそのまま実行して全緑再現+受動変換の独立導出+右手系符号規約の精査(y軸の sin 符号位置まで)。FAIL 2件を裁定: **採用=「座標軸を右(反時計回りに+θ)」の自己矛盾**(右回り=時計回り)→「反時計回り(角度+θの向き)」へ/**棄却=reset が予想ゲートを戻さない**——全単元共通の一方向ゲート設計(setSubmitted(false) を reset に含む単元はゼロ。予想は一度きりの表明であり、答えを見た後の再予想は学習設計上無意味)。全ゲート(typecheck 0・eslint 0・test 1648・build 32p・e2e 94)フレッシュ緑。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 3 第3波「2変数関数の曲面と偏微分」(PR #43)実装+統合 → Tier 3a 3単元完了**。`lib/math/surfacePartial.ts`(4プリセットの解析偏導関数+**中心差分の独立経路**+方向微分+勾配)。**C-7: 解析 vs 中心差分——全プリセットが各軸断面で2次以下の多項式のため Lagrange 剰余の g'''≡0 となり理論誤差が h によらず厳密に0**(実装者が導出、両レビュアーが独立検証で確認。浮動小数点では bit 一致しないため approximatelyZero 判定が正しい設計、と GrokBuild が精査)。独立検証: CI pass。**両レビュアー FAIL→修正で解消(13件目のブロッカー級)**。REVIEWER=GrokBuild=FAIL: **High=checkpoint「同じ点でも向きによって傾きは変わります」が UI 到達可能な臨界点(∇f=0、各プリセットの原点)で偽**——全方向 D_θf=0 で観察表と矛盾(絶対言明破綻の同型)→「ほとんどの点では」+臨界点のとき状態依存の説明追加。Medium 2件反映: 勾配方向 0° 表示の退化明示→「定義されない(この点は平ら)」/ y接線が曲面と同色 0x4f8ff0 で埋没→緑へ。QA_MEMORY=Antigravity=FAIL: **高=座標系——数学の (x,y,z) を Three.js へ恒等写像すると Y-up の世界で高さ z が画面の手前/奥に寝てしまい「山の斜面」の直観が崩れる**(GrokBuild は Low 評価と分かれたが、メタファ成立に関わるため採用)→**単一写像 toSceneCoords(x,y,z)=(x,z,−y)(x軸まわり−90°の回転、det=+1=鏡映でない)を導入し曲面・マーカー・接線・ラベル全てを通す**+軸ラベル Sprite(破棄含む)。中2件反映: 誤解セクションの ∂ 記号を言葉に(記号は形式的定義で初出)/ カメラターゲットを原点へ。小1件反映: 内積説明に単位ベクトル(長さ1)を明記。臨界点 RTL テスト追加。数学コア(偏導関数・方向微分・golden 2√2/√5・転用問題・C=0 主張)は両者 PASS。全ゲート(typecheck 0・eslint 0・test 1687・build 33p・e2e 97)フレッシュ緑。**次: MVP 3 最終=複素関数ドメインカラーリング(まず ShaderMaterial、ADR-005 昇格ゲートで 3b 要否を実測判定)**。 |
| 2026-07-14 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **MVP 3 最終波「複素関数を見る — ドメインカラーリング」(PR #44)実装+統合 → MVP 3 完了・当初構想の全マイルストーン(MVP 1〜3+M4〜M8)完走、公開33単元**。`lib/math/complexFunctions.ts`(複素演算・4プリセット・**windingNumberAround=偏角の数値線積分 vs expectedWindingNumber=偏角の原理の閉形式オラクル=C-7**——square 零点+2/z³−1 各+1/1/z 極−1/mobius ±1 を数値・閉形式・RTL・E2E の全層で突合)。**ADR-005 §5 昇格ゲートの初適用: 実測 60fps・p95 18.6ms+CPU 参照画素比較(平均差 0.00002/255)で Tier 3b 不要を確定——しかも画素比較ゲートが sRGB 二重ガンマの実バグを検出**(LinearSRGBColorSpace へ修正。証跡 assets/adr-005-domain-gate/)。GLSL は表示専用の意図的二重実装(TS が検証の真実)。独立検証: CI pass。REVIEWER=GrokBuild=**PASS**(cDiv・4式の GLSL/TS 静的照合・±π アンラップ・fast-check の「ちょうど1つ囲む」幾何保証・LinearSRGB 判断の正当性まで精査。Medium 3件反映: 境界特異点テストが radius0 の偽検証→実境界ケース(中心(1,0)半径1)へ / **cdiv 経路の E2E 色相検証を reciprocal z=(1,1)→arg −45° で追加**(描画反映前に読む race を状態待ち+double-rAF で解決)/ 厳密零点で arg=0 表示→「未定義(この点は零点)」へ。Low 2件反映: argDeg 値域コメント・catch を RangeError 限定〔バグを隠さない〕。Low 1件=ゲート実測の証跡→assets へ収録)。QA_MEMORY=Antigravity=FAIL→裁定: **採用=(A1 高)予想「グラフに描けるか?」の前からカラーマップ=答えそのものが表示**——「図は常時表示」規範が通用しない特殊ケース(図=答え)→ uShowFunction uniform で**ゲート前は中立グリッド**(e2e に無彩色検証追加)/(A3 中)前提単元に複素除算が未登場→「逆数=大きさは逆数・回転は逆向き」の導入節を形式的定義に追加/(A4 低)cDiv の scale=1 の設計根拠をコメント明記。**棄却=(A2)実験後セクションの露出(規範7例目)/(A5)GLSL に precision 手書き追加——ShaderMaterial は precision を自動注入するため追加すると二重宣言でコンパイル失敗**(RawShaderMaterial でのみ有効な指摘。実コードの precision:'highp' 指定を確認)。全ゲート(typecheck 0・eslint 0・test 1749・build 34p・e2e 100)フレッシュ緑。**5レンダリング方式(SVG/Pixi/Canvas2D/Three.js/GLSL)を同一統治構造で完走**。残: Issue #21・e2e 分割リファクタ・人間ゲート(商標検索・公開告知・教育効果検証)。 |
| 2026-07-15 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree)+ ORCHESTRATOR(Fable 5) | **ポスト全マイルストーンのバックログ3件を全消化(PR #46/#47/#48)**。(1)**PR #46: e2e を単元別ファイルへ分割**(3600行の smoke.spec.ts→helpers+site+lessons/32ファイル。スライス→再構成のバイト単位一致+--list 100件一致で無損失を証明。GrokBuild=PASS〔fullyParallel 既定のため並列度は本質不変と分析〕)——並行波の構造的競合点を解消。(2)**PR #47: Issue #21 幾何 util 共有化**。GrokBuild 初回 **FAIL(Critical)——検証まで共有すると angleAtVertex のゼロ長境界が「点の絶対座標スケール」から「差分ベクトルスケール」へ変わり、巨大座標×近接点の桁落ち領域で旧実装が RangeError で守っていたものが角度を返すようになる**+メッセージ非互換+「テスト無変更で全緑=不変」の証明不足→設計変更: **検証なしの計算核 unsignedAngleFromVectors のみ共有、検証・境界・メッセージは各モジュールが 496fed6 から逐語復元して所有**+旧境界・旧メッセージ・3実装一致の回帰テストで固定→再レビュー PASS(496fed6 との実行意味一致を diff 突合で確認)。修正過程で核関数の自己再帰を混入(置換順序ミス)→26テスト失敗で即検出・修正。**教訓: リファクタの「挙動不変」は計算式だけでなく境界の意味論(スケール定義)とエラーメッセージまで含む——共有すべきは核、所有すべきは契約**。(3)**PR #48: 単元マップ(/map/)**。DESIGN オープン論点の実施(公開32本で条件充足)。クライアント JS ゼロの静的生成(C-3 最強形)、topologicalDepth=深さ計算+**循環・宙ぶらりん参照のビルド時 throw(C-2 の防波堤強化。既存 prerequisites.test.ts は自己参照のみだったため多ホップ循環の独立 DFS 検証も追加)**、SVG は aria-hidden+対話要素ゼロ(aria-hidden-focus の構造的回避、e2e で回帰ロック)+DOM リスト主担保+Okabe-Ito 配色+テキスト併記。深さ分布 0:11/1:8/2:11/3:2。GrokBuild=PASS(循環検出の visiting/depths 分離・ダイヤモンド負例・fail-closed の妥当性を確認。Minor 1件反映)。**実装者が公開単元数の誤記録を検出: 実数32(従来の「33」は sample-math 込みの誤計上)——本行より記録を32に是正**。残タスクは HUMAN ゲートのみ(商標検索・公開ベータ告知・教育効果検証)。 |
| 2026-07-24 | HUMAN + ORCHESTRATOR(Fable 5) | **M9「計測と構造化」フェーズ定義(ADR-006)**。docs/memo.md(キセリョフ〜ソビエト数学教育史の調査対話)を入力に GPT-5.6 Sol × Kimi K3 へ独立戦略協議を委譲(bridge-run: codex-cc:cf10ef50 / kimi-cc:fb7d79b0)、両者が「単元量産の停止・検証と流通への重心移動」で収束 → Issue #49 に統合判断を記録。制作者 Go「被験者いない個人プロジェクトなので自律的に進めて」を受け、P1 を被験者検証から **GA4 行動テレメトリによる破綻検出**(基準の事前コミット必須、効果の証明とはしない=C-5 不変)へ適応。M9a 計測基盤 / M9b 前提チェック関門(単元マップを「見せる可視化」から「通過を要求する積み木構造」へ)/ M9c 演習+予想履歴(localStorage、LLM が提供できない「自分の誤予想との照合」資産)/ M9d 流通(教師向けワークシート・既定学習経路)。やらないこと: 単元数 KPI・アカウント/バックエンド・AI 家庭教師・ゲーミフィケーション(導入には新 ADR 必須)。ROADMAP §4・ADR INDEX 同期更新。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree、ブランチ `agent/m9a-metrics-impl`) | **M9a「計測基盤」実装(Issue #51 第1項、PR未マージ・レビュー待ち)**。手順どおり実装前に `docs/METRICS_PLAN.md` を事前登録(許可リスト4イベント=予想入力開始/予想確定/操作実施/単元完了・送信属性は unit_slug のみ・合格基準のしきい値・最小サンプルサイズ・単元完了の操作的定義=チェックポイント可視1000ms+操作実施済み、を明文化)。実装は `src/lib/analytics/{events,ga4,coreLoopObserver}.ts`(許可リストを型で強制)+ `BaseLayout.astro` への GA4 条件出力(`PUBLIC_GA4_ID` 未設定 or 非 PROD で常に無効。現状は未発行のため全環境で無効)。**設計判断: 全32単元 Experiment Island の共通 DOM 規約(`aria-labelledby="*-exp-title"`/`name="*-prediction"`/確定ボタン文言完全一致/`*-slider`・`*-number`/`<h3>予想と結果</h3>`。grep で全数確認済み)に依拠したドキュメントレベルのイベント委譲とし、32個の Island 自体には一切変更を加えず全単元を計測対象にした**(C-8: 検証済み実装への回帰リスクをゼロにする)。単体テスト13件+e2e3件(window.gtag スタブで実ページに対し4イベントの順序・属性を検証、GA4未設定時の無出力・コンソールエラー0を固定)追加。全ゲート(typecheck 0・eslint 0・test 1826・build 35p・e2e 110)フレッシュ緑。**既知の境界**: `src/pages/index.astro` は独自レイアウトで `BaseLayout` を使わないため GA4/計測の対象外(ホームページ自体に中核ループがないため M9a のスコープ外と判断。後日 GA4 の全体 page_view を揃えたい場合は別途要検討)。次: 独立レビュー(別系統)→統合。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree、同ブランチへ継続 push) | **M9a 独立レビュー(Kimi K2.7、オーケストレータ実コード照合済み)の指摘3件を反映**。**(1) High・実欠陥**: `GraphTheoryExperiment`(辺のON/OFFを SVG の `role="switch"` 要素で操作し、`-slider`/`-number` の `<input>` を一切持たない)だけが旧定義では `experiment_interact` を永遠に発火しなかった。トリガー定義を「予想確定後の実験セクション内の操作コントロール(`<input>` への input/change〔予想ラジオ除く〕、または `button`・`[role="button"]`・`[role="switch"]`・`[tabindex="0"]` への click/keydown〔Enter/Space、予想確定ボタン自体は文言で除外〕)」へ一般化し、`docs/METRICS_PLAN.md` §3 も同時改訂してコードと文書の1:1を維持。**graph-theory-intro の実ページでクリック・キーボード(Space)両経路の発火をe2eで固定**(いずれも green)。**(2) Medium・採用**: 32単元 Experiment(+ importするSceneコンポーネント)を走査し構造契約(実験セクション・予想ラジオ・確定ボタン文言・チェックポイント見出し・計測可能な操作コントロールの存在)を検証する回帰テストを追加(`experimentContract.test.ts`、34件)。チェックポイント見出しを意図的に破壊して実際にFAILすることを手動確認済み(検証後に復元)。**(3) Low・防御**: 操作コントロール判定を実験セクション内(`section.contains()`)へスコープ制限し、旧来の document 全域マッチを解消((1)の一般化と同時に対応)。**任意採用**: IntersectionObserver 対象がドウェル待機中にDOMから切り離された場合の防御(`node.isConnected`)も追加。単体テスト+5件・構造契約テスト+34件・e2e+2件を追加。**全ゲート(typecheck 0・eslint 0・test 1865〔71 files〕・build 35p・e2e 112)フレッシュ緑**。マージはしない(オーケストレータ実施)。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree、ブランチ `agent/m9b-prereq-check-impl`) | **M9b「前提チェック関門(パイロット)」実装(Issue #51)**。**パイロット3単元の選定根拠**: content collection の frontmatter(`prerequisites`)を実走査し、実際に前提エッジを持つ単元(前提チェックは「そのエッジから3問を導出する」ため必須条件)かつ分野が異なる3つを選定——`geometry/trigonometric-ratios`(前提 `pythagorean-theorem`)・`calculus/derivative-function`(前提 `derivative-tangent-line`)・`probability/permutation-combination`(前提 `simple-probability`)。**MVP 1 代表単元の扱い**: MVP 1 の3単元(三平方の定理・微分係数と接線・2×2行列と固有ベクトル)はいずれも `prerequisites: []`(DAG のルートノード)であり、単元自身をパイロット対象にはできない。そこで代わりに、MVP 1 単元を前提として直接参照する下流単元を2つ選ぶことで実質的に組み込んだ(trigonometric-ratios は pythagorean-theorem の内容を検算し、derivative-function は derivative-tangent-line の f(x)=x² の実験例をそのまま再利用する)。固有ベクトル単元には現状これを前提とする下流単元が存在しない(DAG 上の終端)ため対象外——3単元目は分野多様性を優先し、確率分野の `permutation-combination` を選定した。**実装形態**: 再利用可能な単一 Island `src/components/lesson/PrerequisiteCheck.tsx`(`client:only="react"`——JS 無効時は SSR に一切現れず「表示されないだけ」を保証、他の Island の `client:visible`+`<noscript>` パターンとは異なる新規パターン)+ 単元別の colocated データ `src/lib/prerequisiteChecks/{trigonometricRatios,derivativeFunction,permutationCombination}.ts`(frontmatter ではなく TS モジュール。content.config.ts のスキーマは無変更=C-2)。**正誤判定への lib/math 再利用**: 3単元とも既存の数学モデルで検算——`pythagoras.ts`(`squaredDistance`)で斜辺・三つ組の妥当性、`derivative.ts`(`derivativeAt`/`differenceQuotient`/`tangentLine`)で微分係数・割線収束・接線の式、`probability.ts`(`theoreticalProbability`/`relativeFrequencies`)で理論確率・相対度数を検算し、正答IDをハードコードではなく計算結果から決定する `pickCorrectChoiceId` ヘルパーで導出(ちょうど1つ正解でなければビルド時例外=C-7)。各問に出典(前提単元のどの概念か)と正答根拠(どの lib/math 関数で検算したか)をコードコメントで明記(数学 QA が検証しやすい形)。**不正解・自信なし時の誘導**: 各問に「わからない/自信がない」選択肢を追加し、採点後に1問でも不正解/自信なしなら前提単元への相対リンク(`../{slug}/`)を提示。**強制ブロックしない**: 「スキップして本文へ進む」ボタンでパネルを畳め、「もう一度確認する」で再表示可能(既定は展開表示)。**GA4 スコープ回帰(M9a との整合)**: 外枠 `section` の `aria-labelledby="prereq-check-title"` は `coreLoopObserver.ts` の `*-exp-title` 選択子にマッチせず、ラジオ `name` も `-prediction` で終わらないため `experiment_interact` 等は構造的に発火しない。jsdom 統合テスト(`src/lib/analytics/__tests__/prerequisiteCheckScope.test.tsx`)で固定し、実ブラウザでも e2e で追加検証(trigonometric-ratios.spec.ts)。**既存資産は無変更**(C-8): 3記事の既存本文・既存 Island・`content.config.ts`・`[slug].astro` はいずれも変更せず、mdx へは import 3行+コンポーネント呼び出し1行の純追加のみ。**テンプレ化判断(全単元展開の可否)**: 3単元を通して、(1) データ形式(colocated TS + `pickCorrectChoiceId`)は前提単元が1つの単元には低コストで複製可能——ただし数値問題を lib/math で検算できる単元(今回の3例)は前提が明確な数式・恒等式を持つ場合に限られ、概念的・定性的な前提(例: 図形の相似の直感、グラフの概形の識別)を持つ単元では「検算可能な数値問題」への翻訳に追加の設計判断(何を数値化するか)が要り、単純複製はできない。(2) 複数前提(例: `circle-equation` の `[pythagorean-theorem, quadratic-equation]`)を持つ単元は今回未着手で、UI(複数の前提単元リンクをどう提示するか)・データ形式(問題ごとに出典前提が異なる場合の表現)の拡張が別途必要。(3) 問題作成コストは1単元あたり「前提単元の再読込+lib/math 関数の選定+3問の設計+単体テスト」で実装者換算約30分〜1時間規模(小規模)だが、**数学的正しさの担保(出典・根拠のコメント、独立再検算テスト)に費やす比重が高く、量産よりも前提エッジが曖昧な単元から優先着手する方が費用対効果が高い**。→ 結論: **全単元への機械的展開はしない**(ADR-006 のスコープ外明文化と整合)。M9c(演習+予想履歴)は同一パイロット3単元への追加実装として着手し、DAG 上で前提エッジが明確かつ数値検算可能な単元から漸進的に広げるのが妥当と判断する。全ゲート(typecheck 0・eslint 0・test **1894**〔74 files〕・build 35p・e2e **116**)フレッシュ緑(`npm install` 未実行の worktree だったため最初に依存関係を再インストールして検証)。契約回帰テスト(`experimentContract.test.ts`)は無変更のまま緑を維持(PrerequisiteCheck は `*Experiment.tsx` の走査対象に含まれないため無関係)。マージはしない(オーケストレータ実施)。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree、同ブランチ `agent/m9b-prereq-check-impl` へ継続 push) | **M9b 独立レビュー(数学QA=Antigravity=PASS〔全9問検算一致〕、コードレビュー=Kimi K2.7=CONCERNS〔Critical/High 0〕)+オーケストレータ照合で検出された追加指摘4件を反映**。**(1) High・実欠陥(オーケストレータ検出)**: 全9問の正答がすべて選択肢の先頭(表示順1番目)に固定されており、「常に最初を選ぶ」だけで前提チェックを通過でき診断力が無効化される欠陥を修正。実行時シャッフルは SSG/hydration の非決定性を避けるため採用せず、**データ上で正答位置を静的に分散**(`pickCorrectChoiceId` は順序非依存のため、各データファイルの choices 配列の並び順のみを変更し、id と値の対応・正誤判定ロジックは無変更)。9問の新分布は **a:2 / b:2 / c:3 / d:2**(スクリプトで実測)。回帰防止として `dataCorrectness.test.ts` に「正答位置が同一ではない」「特定位置に偏り過ぎていない(過半数超えない)」の2ガードテストを追加。**(2) Medium**: 設問フィードバック(正解/要見直し)とサマリー(採点結果)に `aria-live="polite"` を付与し、スクリーンリーダーが動的な採点結果を読み上げるようにした。**(3) Medium**: `.dismissedNotice button`(スキップ後の「もう一度確認する」ボタン)が `.check` のスコープ外にレンダリングされるためプロジェクト統一の focus-ring が当たっていなかった欠陥を修正(`:focus-visible` ルールを追加)。ついでに同ボタンの文字色も `.summary a` と同じ理由(コントラスト不足の実例が既にあったため)で accent-primary の青ではなく地の文の高コントラスト色+下線に変更(予防的措置)。**(4) Medium**: `derivativeFunction.ts` の Q3(接線の式)が `choice.id` による分岐(`if (choice.id === 'a') return ...`)で正誤を決めており、id と数値の対応がずれても検出できないリスクがあった。各選択肢に `slope`/`intercept` を直接持たせ、`tangentLine` の計算結果と内容ベースで比較する形に修正(id の並び順にも依存しなくなり、(1)の位置分散とも自然に両立)。**(Low・任意、明示コメントで対応)**: スキップ→再確認時に回答状態をリセットしない(コンポーネントをアンマウントしないため保持される)のは意図的な設計であることをコード内コメントで明記(独学者が選び直した回答を毎回失わせない配慮)。value フィールドの型は全データファイルで `number` に統一済みであることを確認(追加変更なし)。全ゲート(typecheck 0・eslint 0・test **1897**〔74 files〕・build 35p・e2e **116**)フレッシュ緑。マージはしない(オーケストレータ実施)。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、隔離 worktree、ブランチ `agent/m9c-exercises-history-impl`) | **M9c「演習+予想履歴」実装(Issue #51)**。**Part A: 演習(パイロット3単元 × 各5問)**。M9b と全く同一のパイロット3単元(`geometry/trigonometric-ratios`・`calculus/derivative-function`・`probability/permutation-combination`、ADR-006 の指定どおり)を対象に、各単元末尾(実験・転用問題〔チェックポイント〕の後)へ演習を追加。**設問は前提単元ではなく単元自身の内容の理解確認**(M9b の前提チェックとは向きが逆)。再利用可能な単一 Island `src/components/lesson/ExerciseSection.tsx`(`client:only="react"`、PrerequisiteCheck と同型のパターン)+ colocated データ `src/lib/exercises/{trigonometricRatios,derivativeFunction,permutationCombination}.ts`(`src/lib/prerequisiteChecks/types.ts` と同型の `pickCorrectChoiceId` を独立複製、モジュール分離のため)。**正答は全問 lib/math の既存関数で検算**(trigonometry.ts の `unitCirclePoint`/`sine`/`cosine`/`tangent`〔cosθ≈0の実例外捕捉で正誤判定〕、derivativeFunction.ts の `toDifferentiableFunction` 経由で derivative.ts の `derivativeAt`/`tangentLine`、combinatorics.ts の `permutations`/`combinations`)。数値化しにくい概念設問(sinθの値域・θ増加時のcosθの挙動)も、決め打ちではなく0°〜360°の多点サンプリングによる実測(単調性・最大値の実測)で正誤を導出。**誤答パターン別フィードバック**: 全15問・全誤答選択肢(計45個)に「なぜその誤答に至るか」の説明文(`misconception`)を設定し、選んだ誤答に応じて即時表示(ガードテストで全誤答に misconception が存在することを固定)。**正答位置の分散**(M9b の教訓を最初から反映): 15問を4つの表示位置に分散(0:4問・1:4問・2:4問・3:3問)し、`dataCorrectness.test.ts` に「同一位置ではない」「過半数〔8件〕を超えない」の回帰ガードを M9b と同型で追加。**GA4非干渉**: 外枠 `aria-labelledby="exercise-section-title"` は `*-exp-title` に、ラジオ name も `-prediction` にマッチしないため構造的に非干渉(jsdom統合テスト`exerciseSectionScope.test.tsx`+3単元のe2eで実ブラウザ固定、`experimentContract.test.ts` は `*Experiment.tsx` 走査のため無関係のまま緑)。既存3記事の本文・既存Island・`content.config.ts`は無変更(mdxへの追記はimport 2行+コンポーネント呼び出し1行のみ、C-8)。**Part B: 予想履歴(localStorage)**。32単元共通の構造契約(name=\"*-prediction\"・確定ボタン文言完全一致、M9a coreLoopObserver.ts と同一契約を再利用のため `getUnitSlug`/`PREDICTION_RADIO_SELECTOR`/`SUBMIT_BUTTON_TEXT` を coreLoopObserver.ts から export)に依拠した document 委譲 `src/lib/analytics/predictionHistoryRecorder.ts`(確定ボタンクリック時に checked ラジオの value と、それを包む `<label>` のテキストを記録)+ ストレージ層 `src/lib/analytics/predictionHistory.ts`(キー **`nabla:predictions:v1`**、バージョン付き。読み取り/書き込み/削除いずれも try/catch で無言フォールバックし例外・console.error を一切出さない、容量超過・プライベートモードを想定したテストで固定)。`BaseLayout.astro` に `initPredictionHistoryRecorder()` を追加するのみで**32単元の既存Islandは無変更**。専用ページ `/history/`(`src/pages/history/index.astro` + Island `PredictionHistoryView.tsx`)で単元・選んだ予想・確定日時を新しい順の表で一覧表示し、**JSONエクスポート**(`{version:"nabla:predictions:v1", exportedAt, records}` を Blob ダウンロード、ファイル名 `nabla-predictions-export.json`)と**全削除**(2段階確認、ネイティブ`confirm()`は使わずUI内で「本当に削除しますか?」→「削除する」/「キャンセル」)を実装。トップページに導線リンクを追加(`index.astro`)。プライバシー: `docs/METRICS_PLAN.md` §2 に「予想履歴はローカルのみ・GA4を含めどこにも送信しない・許可リストを拡張しない」を明記(タスク仕様どおり)。**環境上の発見**: Node.js v22以降の `--experimental-webstorage`(v25では既定有効)により、`--localstorage-file` 未指定時 `globalThis.localStorage` が getItem/setItem 等を一切持たない壊れたスタブになり、jsdom 環境の vitest でもこれを引き継いで `window.localStorage` が機能しない(実ブラウザ・Playwright実行には存在しない、Node起動時のみの問題)。ソースコードを変更する対処ではなく、`vitest.setup.ts` にフィーチャー検出(`clear` が関数でない場合のみ)で最小限のインメモリ `Storage` ポリフィルを注入する形で解決(本物のブラウザ実装が使える環境では何もしない)。**a11y修正**: `/history/` は index.astro/map.astro と同じ明るい背景のページのため、当初 PrerequisiteCheck 等のダーク surface 用フォールバック色(`--nabla-text-secondary, #a7adc0` 等)を誤って流用し axe で color-contrast(Serious、実測比 2.23、要求4.5:1)を検出。ダークsurfaceトークンを流用せず、index.astro と同系統の色(`#444`/`#9c2b1c` 等、実測 7.5:1 以上)に総入れ替えして解消。**品質ゲート**: typecheck 0 errors(246 files)/ eslint 0 / test **1959件**(80 files、うち本タスクで追加した新規テストのみで dataCorrectness 24件・ExerciseSection component 7件・exerciseSectionScope 4件・predictionHistory 11件・predictionHistoryRecorder 7件・PredictionHistoryView 7件)/ build 36 pages(`/history/` 追加分含む)/ e2e **126件**(3単元それぞれの演習表示・a11y二段階・GA4非干渉+予想履歴の記録・一覧・エクスポート・全削除の新規e2e追加分含む)。`experimentContract.test.ts` は無変更のまま引き続き緑。`npm install` 実行時に `three`/`pixi.js` が未インストールだった(package.json記載はあるがnode_modules欠落、環境起因)ため最初に再インストールして解消(ソース変更なし)。マージはしない(オーケストレータ実施)。 |
| 2026-07-24 | IMPLEMENTER(Claude Code Sonnet、同ブランチ `agent/m9c-exercises-history-impl` へ継続 push) | **M9c 2系統独立検証(コードレビュー=Kimi K2.7=CONCERNS〔High1/Medium3/Low3〕、数学QA=Antigravity=CONCERNS〔全15問の計算は正しいが misconception 文品質 Medium4/Low3〕)の指摘を反映**。**コード側(1) High**: trigex-4(sinθ最大値)・trigex-5(cosθ単調減少)の正誤判定が0°〜360°/0°〜90°のサンプリングのみに依拠しており、レビュー指摘どおり「サンプリングはlib/mathへの実測確認に過ぎず数学的証明ではない」という弱点があった。**exact な golden 検証を追加**: trigex-4 は θ=90° の厳密値 `sine(90°)=1`(単位円の有界性、golden)を数学的根拠として直接検算し、361点サンプリングはこれと一致することを確認する補助ガードへ格下げ。trigex-5 は 30-60-90/45-45-90 直角三角形から幾何学的に証明可能な特殊角の厳密値(`cos0°=1 > cos30°=√3/2 > cos45°=√2/2 > cos60°=1/2 > cos90°=0`、lib/mathに依存しないJS定数)を golden とし、この並びの狭義単調減少をまず確認、次に lib/math の `cosine()` が同じ厳密値を返すことを検算、7点サンプリングは補助的な追加確認に格下げ。**(2) Medium**: `PREDICTION_RADIO_SELECTOR`/`SUBMIT_BUTTON_TEXT`/`getUnitSlug` を GA4専用の `coreLoopObserver.ts` から `predictionHistoryRecorder.ts` が拝借する不自然な結合を解消するため、中立モジュール `src/lib/predictionContract.ts` へ切り出し、両モジュールがそこから直接importする形に変更(`coreLoopObserver.ts` はもう再輸出しない)。単体テスト`predictionContract.test.ts`を追加。**(3) Medium**: 確定ボタンの連打・二重クリックで同一内容の重複レコードが履歴に積み重なるリスクに対し、`prediction_submit`のfireOnceと同型の「1ページロードにつき最大1件」ガード(`hasRecorded`フラグ)を`predictionHistoryRecorder.ts`に追加。`docs/METRICS_PLAN.md`にも仕様として明記。回帰テスト2件追加(連打で1件のまま・選び直して再確定しても最初の内容のまま増えない)。**(4) Medium**: `/history/`の全削除2段階確認のa11y修正——確認UI出現時に「削除する」ボタンへフォーカス移動、キャンセル時は「すべて削除」ボタンへフォーカスを戻す(`useEffect`+`wasConfirmingRef`で確認状態の遷移を検知、クリックハンドラ内の直接`.focus()`はReact再レンダー前に実行され対象が見つからない実装ミスをテストで発見し修正)、確認グループに`aria-live="polite"`を付与。回帰テスト3件追加。**数学QA側(misconception文の書き直し、正答・計算は全問維持)**: trigex-3選択肢b(学術的な「残差」表現→「恒等式の右辺の値1を0と勘違い」に平易化)/ trigex-5選択肢c・d(内部検証用語「実測では」の漏出→「単位円上の点のx座標の動き」という概念的説明に書き換え)/ derivfnex-3選択肢d(「符号誤り」表現→「元の関数の値f(-1)=-1と微分係数の混同」に修正)/ derivfnex-4選択肢c(**説明と数式の不整合を発見**——「傾きをf(1)=1と取り違えている」という説明から実際に導かれる式は選択肢だった`y=x-1`ではなく`y=x`だったため、選択肢の値自体を`y = x`〔slope=1, intercept=0〕へ修正して整合させた。正答位置分散ガード・他選択肢のslope/intercept重複なしを確認済み)/ derivfnex-5選択肢c(「たまたま近い値」という曖昧な表現→「交点(1,1)でf(1)が等しいことと微分係数が等しいことの混同」という具体的な数学的事実に修正)。任意(Low)としてpermcombex-3選択肢d・permcombex-4選択肢dの説明も軽微に補強。任意(Low)のvitestポリフィル実ブラウザ差異コメント拡充・`pickCorrectChoiceId`共有ヘルパー化・aria-live集約は、M9bの既存モジュール(`prerequisiteChecks/types.ts`)への不要な追加変更を避ける判断(C-8)+効果対コストの観点から見送った(オーケストレータ判断を仰ぐ)。**全ゲート(フレッシュ実行)**: typecheck 0 errors(248 files)/ eslint 0 / test **1968件**(81 files、本ラウンドで追加した新規テストのみで predictionContract 4件・predictionHistoryRecorder +2件・PredictionHistoryView +3件)/ build 36 pages / e2e **126件**(既存全件が新しい選択肢・misconception文・golden検証・フォーカス管理を含めて回帰なしで通過)。マージはしない(オーケストレータ実施)。 |
